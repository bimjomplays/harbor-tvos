# PROJECT_STATE — Harbor for tvOS

## Goal
Native Apple TV app with full Harbor (beta-branch) feature parity, same Harbor account, shipped by TestFlight, no physical Mac.

## Status (2026-09-23 09:25 EDT)
> **Times:** the `HH:MM (09-24)` / `HH:MM (09-25)` labels on the entries from the overnight session are a running sequence, not wall-clock times. That session really ran 2026-09-23 23:30 → 2026-09-24 06:44 UTC (149 commits; `git log` has the real times), so every entry labelled (09-25) happened on 09-24 UTC. New entries from 2026-09-25 on use real UTC times (the commit time of the log commit).
Repo public; CI green on every push tonight; TestFlight builds dispatched after each (latest ≈ build 70; check App Store Connect). User's TMDB key still unresolved (Settings → Artwork and rows → **Test saved key** prints TMDB's answer).
Built tonight (all simulator-tested, none device-tested yet): Stage 3 detail page (episodes, seasons, credits, watchlist, Resume label + progress, watched marks from the Stremio bitfield), stream picker (flat cached-first list, quality/Cached/addon chips), debrid resolve, player (chrome, seek, pause, audio/subtitle panels, online subtitles via OpenSubtitles/Wyzie/addons, up-next pill, next-episode advance, resume + 4 s progress saves to local + Stremio), Addons manager, Library room, Anime room (Jikan), Live TV slice (M3U playlists → channel grid → live mpv mode), onboarding layout + subtitles steps, subtitle-language setting. Three fresh-context Sonnet reviews applied (10 fixes incl. mpv teardown race, zlib vs raw deflate for watched bitfields, Set→array across the JSON bridge, settingsLinked).
Morning 2026-09-23 (user asleep, "keep working"): skip intro/outro/recap pill (engine `skip.segments` over AniSkip/SkipDB/TheIntroDB/IntroDB App), display-mode matching via `AVDisplayCriteria(refreshRate:formatDescription:)` + `UIWindow.avDisplayManager` (AVKit; only acts when the viewer's Match Content is on).
Also: preferred audio/subtitle language auto-select (ISO 639-2 alias table), subtitle style → mpv, Discover Awards band + award detail (offline catalog `App/Engine/awards.json` copied by tools/sync_upstream_assets.sh and installed into the engine on first use) and Top People band (harbor.site rank API).
09:00: Trakt device-code connect (Settings → Trakt; engine `trakt.*` over lib/trakt via Harbor's token proxy) + player scrobbles (start/pause/stop ≥90%); profiles now persisted in upstream's `harbor.profiles.v1` `{activeId, profiles}` shape through KeyValueStore and mirrored into the engine (`syncStorage` + `harbor:profiles-updated` / `harbor:active-profile-changed` events); upstream secret prefixes routed to the Keychain.
09:20: Collections room (Settings-free: `collectionsRoom.mine/community/all` over lib/collections; 6-col card grid → items overlay → detail). Fourth/fifth reviews applied: Trakt poll timer cleared + de-duplicated per device code, durable-tier `get` falls back to the cache store, Trakt poll loop drops stale results. All on TestFlight (`Build` run of 09:21 EDT).
11:00: Two-way profile sync + engine-owned account session. `engine/sync.ts` wires upstream lib/profile-sync + layout-sync + TV-panel sections onto localStorage (roster apply → rewrites `harbor.profiles.v1` + `harbor:roster-applied`; settings apply → `harbor:settings-updated`; status → `harbor:sync-status`); `engine/account.ts` wraps theme-auth/identity (login/register/logout/token/refresh runner, `harbor:account-changed`). Swift: AccountStore/SyncReader are engine mirrors, ProfilesStore reloads on roster-applied, seeded profile is `bootstrap: true` (dropped on first pull instead of duplicated), id map migrated to the KeyValueStore string form. Smoke covers adopt/drop/purge, session carry-over, active-profile apply, push at baseRev.
12:00: Card marks (bp-card-marks/state-marks): `engine/cards.ts` computes per-row identity chip (bundled Oscar/Emmy… + anime award + DUB + New/Rerun/In Cinema), watchlist bookmark zone, watched check (opposite corner to badgePlacement), Top 10 ribbon (rooms feed `setTop10Metas`; `App/Upstream/marks/toptab{l,r}.png`). Swift `CardMarksStore` (Browse/CardMarks.swift) refreshed after room/search/library/catalog loads, watchlist toggles and playback end; `BPTileView` overlays via `CardMarksOverlay`. No score chips on TV tiles (upstream gate). Review fixes: roster apply emits `harbor:active-profile-changed`, shell returns to who-is-watching when the active profile vanishes, id-map migration mirrored into the engine, API errors cross as `harbor-api:{json}`.
13:00: Live TV v2 on upstream's IPTV stack: `engine/live.ts` = sources via `detectProviderShape` (M3U / middleware probing / Xtream login URLs with derived xmltv.php EPG), `loadPlaylist` cache, favorites (`harbor.iptv.favorites.v2`), pins/stats, `bpGuideOrder`, XMLTV guide (`loadEpg` with gzip via fflate, `nowNext`, `schedule` through upstream's epg-resolver). Response shim gained a one-chunk `body` stream (upstream's bounded readers need it). Swift `LiveView`: source + Favorites/All/group chips, guide-lite rows (logo, now + progress, next, star), Sources sheet (add with optional EPG URL, switch, remove, set EPG, refresh). 
13:30: Simkl: `engine/simkl.ts` (PIN sign-in via lib/simkl/device-auth, session in Keychain tier, `scrobble` honouring `simklScrobbleEnabled`); Settings → Simkl reuses `TraktPanel(service:label:)`; player scrobbles to Trakt and Simkl.
14:00: Guide grid (`Live/LiveGuideView.swift`, bp-guide): channel column with the star as the only focusable, half-hour ruler with day hint + Now pill, gapless lanes from `live.lanes` (buildLane port), past/airing/future blocks with width tiers + chevrons + airing progress, now line, focus-driven panning (viewStartFor) and window growth (+3 h, max 26 h). Live screen defaults to the grid when the source has a guide; List/Guide toggle in the band.
14:30: Anime4K: `engine/anime4k.ts` (shader list from anime4k.rs, gates from use-anime4k.ts: playerAnime4k/AnimeOnly/Mode/Tier/Override, display-width demotion AA→A, performance→fast); Swift `Anime4KStore` downloads the 11 GLSL files into Application Support; `MPVPlayerController.setShaders` sets `glsl-shaders`; player applies on file load (source width from `video-params/w`), chip + panel (Auto/Off/modes → `playerAnime4kOverride`), corner indicator; Settings → Anime4K panel. Untested on the device: shader compile under MoltenVK/libplacebo and HQ performance.
15:30: Sports room (Stage 11 MVP, docs/sports-spec.md): `engine/sports.ts` runs upstream's hub (fetchHubSlice + loadSportsSlices, ESPN + free feeds, no key) with a memory slice cache, cache-first `page()` that refreshes in the background and raises `harbor:sports-updated`; consent, catalog, personalization (`setLeagues` writes settings.sportsLeagues + favourites), row shaping (bp-sports-rows), featuredEvents, hot events, `detail` (ESPN summary). Swift `App/Sources/Sports/`: consent notice (upstream copy verbatim), chips (modes/groups), date band, hero with 7 s cycle, rows + cards (BpSportsCard rules), Explore grid, empty states, event detail (stats bars, play-by-play, lineups), personalize (sports → leagues). Not yet: teams step, api-sports key, artwork layering.
16:00: Sports watch flow (§2.4): `sports.watch` builds upstream's sports channel index from every Live TV source (`buildSportsChannelIndex`), scores with `matchChannelsForGameAsync` (attached pins in `harbor.sports.sources.v1`, broadcast names), lists watch providers + organizer Twitch broadcasts as information; plan channel/picker/setup/finished. Event view: Watch button, picker with tier copy + Pin for the competition, plays through PlayerScreen (live, channel headers). Sixth review applied: Simkl PIN expiry (engine clock + Swift deadline), guide lanes swap atomically with a generation guard and gap cells are focusable, Anime4K only claims active when shaders exist and drops a chain mpv rejects (log scan), Simkl scrobble toggle, card marks use the sync TMDB→IMDb cache.
16:30: Big Picture settings parity (Stage 9): `engine/settingsRoom.ts` exposes upstream's bp-settings-catalog (8 categories with summaries, option/multi/push/action controls) and a commit port of bp-settings-commit (services toggle, subtitle languages, overscan, sports tab gate → consent reset/decline). Swift `Settings/BPSettingsView.swift` renders the category column + control rows at the top of Settings; Sports tab hides when declined (`SettingsBridge.sportsDeclined`). 
17:00: Profile management: `engine/profilesRoom.ts` (upstream avatar catalog + brand colours + per-profile purge); Swift `ProfilesStore.create/update/delete` (delete tombstones through `sync.profileDeleted` first, never the primary), `Profiles/ProfileEditorView.swift` (name, colour, 65 bundled avatars) from Settings → Profiles (Add / Edit). Overscan setting now insets the whole shell.
17:45: Library room v2 (use-bp-library port): `engine/library.ts` tabs (Saved/Watchlist/History/My Lists/Favorites + Trakt/Simkl when connected), per-tab merge rules (mergeWatchlist/filterLibrary/mergeHistory/historyItemsToDated, favorites map, custom lists, Simkl cache), type/query/group filters, sort→sections→cap; Swift `LibraryView` tabs, filter rows, search, sectioned poster grid with history progress. **Bundle-size fix:** importing `@/lib/i18n` from settingsRoom had inlined every locale catalog + the 4 MB awards JSON (esbuild inlines dynamic imports) → 27.8 MB engine since the 11:00 build; `bundle-config.mjs` now stubs `load-locale` and `@/data/awards.json` (2.37 MB again). Seventh review applied: sports `empty` needs `!busy`, failed-feed labels deduped, channel index re-keys on fetchedAt, SportsModel reload generation guard, settings `sportsNotice` opens Sports, KeyValueStore durable tier now covers `harbor.settings*`, sports favourites/sources, consent (`harbor-` prefix keys included in snapshots), Live TV playlists/favorites/pins, installed addons.
18:15: Anime room v2 (use-bp-anime port): `engine/animeRoom.ts` loads the 16 Jikan spec rows progressively (Jikan's own queue), anime Continue Watching (local + cloud, one per franchise), upstream's seeded hero selection, top picks from Top Airing, award winners from the bundled index, addon anime catalogs streaming in, collections on the anime page, row customization; ranked rows use the rank tile. Swift: EngineBrowseSource `.anime` reads `animeRoom.page`, BrowseModel re-reads on `harbor:anime-updated` (400 ms debounce, pending-reload guard), anime CW through `continueWatching(for:)`. MAL/AniList rails wait for a TV sign-in flow. Fixed: ProfileEditorView `try? await` inside a `??` autoclosure (CI compile error).
18:45: Streaming services (Stage 2 remainder): `engine/services.ts` (enabled brand tiles when a TMDB key exists, poster mosaics, per-service category rows via service-catalog with 4 lanes + 5 s caps, paging); Swift `.brand` tile shape (tinted 1.2:1 tiles), the Home "Your streaming" band after row 2, `ServicePageView` (RoomView over `ServiceBrowseSource` with its own cache slot), See-all paging routed for `svc:` and anime spec rows. Lights up once the TMDB key works.
19:15: AniList + MyAnimeList: `engine/trackers.ts` (authorize URL → paste code → upstream completeAuthorization via Harbor proxies; status/disconnect; status rails with cache-first loading raising `harbor:anime-updated`; Library tab entries). Anime room shows Your AniList / Your MAL rails when connected; Library gains the tabs. Swift `Trackers/PasteTrackerView.swift`: QR (CoreImage) + link + paste field (iPhone remote keyboard pastes) in Settings → AniList / MyAnimeList.
19:30: Live TV catch-up (beyond Big Picture, from desktop use-live-actions): `live.lanes` flags catch-up channels, `live.catchupUrl` builds flussonic / Xtream timeshift / template replay URLs; the guide grid plays a past programme as a bounded replay (REPLAY chip, VOD cache profile, seekable). Search now also shows Live TV channel hits (playable) and one row per addon that answered (`search.all` already returned both).
20:00: Detail page TMDB extras (Stage 3 remainder): `engine/detailRoom.ts` (tmdbDetails → tagline, cast portraits, crew groups, More Like This / You Might Also Like, facts card rows, watch-on providers, franchise collection). DetailView shows tagline, Watch on marks, TMDB crew lines, Cast row, Collection / recommendations / similar rows (open nested detail), Details card. All null without a working TMDB key.
20:30: Person page (bp-person port): `engine/personRoom.ts` (tmdbPerson → facts, Known For, IMDb Top, filmography sections with sort/min rating, bundled awards, desktop's collaborator ranking cache-first with a background fetch raising `harbor:person-updated`); Swift `Detail/PersonView.swift` opened from Search people cells and Detail cast cards, collaborators open further people.
20:00 (09-24): Integration fixes (subagent, from a cross-feature runtime review): the Together room is an overlay inside PlayerScreen (no cover), mpv/AVPlayer tear down in dismantleUIViewController/deinit instead of viewDidDisappear (mpv teardown runs once); ThemeStore holds theme snapshots and RootView keeps its built language while PlaybackState is active; curfew lock and screensaver live in their own key UIWindow above every cover (`ShellOverlay` in Screensaver.swift); Multiview tiles stop when the app isn't active; a music track resolving during video is queued paused; persisted liked/recent music tracks carry no Subsonic u/t/s or Plex token (re-applied at read time, cleaned on disconnect), liked songs and manga favourites durable, a durable write clears the older tier copy; hiding the current tab / manga off / Sports declined sends you Home (hidden rooms still open from Settings/Search/deep links), LB/RB from an off-bar room jumps to the ends; `PreviewGate` stops Home/Guide live previews during playback, under any cover, the screensaver or the curfew lock, and off screen; Together relay change resets the snapshot explicitly. Needs device: the overlay window's focus handoff, Room chip mid-play, covers over the player no longer stop playback, previews under the account menu, Multiview on leaving the app, display mode reset on player close. Offline smoke 529.
21:00 (09-24): Stage 12 Spotify (subagent): librespot 0.8 in harbor-ffi behind `harbor_spotify_*` (ring-buffer sink drained by AVAudioEngine in `SpotifyPlayback.swift`, which never deactivates the shared audio session; upstream's patched librespot-core vendored; `open` stubbed for tvOS in `rust/vendor/open`; vergen 9.0.6; rustls/webpki, no audio backends), sign-in with the listener's own client ID through the phone (QR + paste-back of the redirect address), Web API browse/search/album/artist/playlist and token refresh in `engine/musicSpotify.ts`, MusicPlayer routes Spotify entries through the shared queue/Now Playing/remote commands/scrobbles/failover, Connections row and setup sheet, keys in the Keychain. Type-checked for tvOS(-sim) on the host (nightly build-std); CI screens job timeout 75 min for the first rebuild. Staticlib (host) 110 → 207 MB. Skipped: Spotify library page and playlist writes, Spotify Connect, artist albums past ten, volume, source label. Needs CI + a Premium device check (paste-back on phone browsers, HDMI/AirPlay audio, background, pause/seek, track advance, re-sign-in after sleep). Bundle 4042 KB, offline smoke 550.
21:30 (09-24): Review 23 (Spotify, fresh context; no compile errors found by hand): a disconnect during sign-in now sticks (generation re-checked after the token and sessionReady awaits; the engine forgets the sign-in again, and a stale attempt leaves Rust to disconnect()'s queued shutdown), any error after the Rust connect shuts that session down and is recorded, a finishing restore only clears its own task, a Spotify track resolving during a film is queued paused, a pasted redirect address must carry the flow's state (only a bare code goes without), the ring sink is owned by the newest Player (a retired one's writes are dropped; retire flushes) and a flush drops the rest of a packet mid-copy (no stale audio after seek), the audio session is re-activated before the engine restarts after an interruption. Left: the engine keeps rendering silence while Spotify is paused. Rust tests + 2 new ring tests pass; offline smoke 550.
22:00 (09-24): AVPlayer subtitle renderer and PiP (subagent): the AVPlayer engine reads the file's own subtitle track through AVPlayerItemLegibleOutput with AVPlayer's drawing off, kept as a timeline and drawn by NativeSubtitleOverlay at currentTime − offset, so Sync and Look work for embedded and added subtitles on both engines (still selected through AVMediaSelectionGroup; "2nd" on AVPlayer only for added subtitles). Picture in Picture on the AVPlayer engine: an AVPictureInPictureController on a separate AVPlayerLayer of the same player (the AVPlayerViewController stays for picture-mode matching). PlayerEngineControlling gains supportsPictureInPicture / isPictureInPictureActive / start/stopPictureInPicture (false / no-ops on mpv). "Picture in Picture" button only on engines that support it, a panel while PiP is on (Exit Picture in Picture / Leave), Back exits PiP first, no pause in the background during PiP, subShowInPip lets AVPlayer draw the embedded track inside the PiP window. Not done: browsing Harbor while PiP plays, added subtitles inside the PiP window. Needs device: embedded subtitles through the overlay (tx3g, HLS WebVTT, CEA-608), PiP start/stop/background, HDR/DV matching with the second layer. Offline smoke 550.
22:30 (09-24): Stage 12 Spotify batch 4 (subagent): the library page (`library.rs` / `music-spotify-library.tsx`: Spotify playlists and Liked songs 50 a page with Load more, upstream's `next`-link checks, local/unavailable/episode entries skipped and counted, owned or collaborative playlists opened in place, others as an open.spotify.com QR code, private playlist creation, permission/reconnect notices) behind a "Spotify library" button on Music home while connected (`MusicSpotifyLibrary.swift`). Add to playlist from the hold-Select track menu for Spotify tracks (only editable playlists, snapshot id required, create then add). Artist albums paged ten at a time with `artist_catalog.rs`'s artist-bound cursor (Load more tile). SpotifyPlayback stops its output after a pause and MusicPlayer's 250 ms clock stops while nothing plays, both restarting on resume. Engine `music.spotifyLibraryPage / spotifyCreatePlaylist / spotifyAddToPlaylist / artistMore` (errors as upstream's `spotifyLibraryErrorKey` keys). Skipped: Import to Harbor (no Harbor playlists), like/unlike (upstream has none), saved-albums/followed-artists views (not in upstream's page), the "Premium · 320 kbps" label (never rendered upstream), Connect, volume. Needs device: the Add to playlist menu item, library paging/create/add, reconnect for permission, pause/resume latency with the output stopping. Bundle 4054 KB, offline smoke 566.
23:00 (09-24): Translation coverage 3 (subagent): Swift copy that upstream has a key for now uses upstream's wording or format keys (Live TV setup labels per bp-live-setup, skip pill per bp-skip-pill, the Suwayomi form heading, eBook facts/cards/"More by", airing countdown "in {time}", sports "Yard line"/"Bench", the API-Sports {football} note, subtitle "Show {count} more"), and String-typed copy shown verbatim goes through T() (eBook rail titles, skip pill, calendar result pill/skeleton, manga/ebook TV notes, watch-together quick replies). New `tools/locales-tvos.json` (98 Apple TV strings upstream lacks, all 15 languages; translations written by Claude, NOT reviewed by native speakers) is merged by build_locales.mjs after upstream's keys (upstream wins, placeholders checked, Swift format versions generated). Kids Play Zone copy stays English like upstream's. Coverage (de, tools/l10n_coverage.mjs): Kids/Manga/Live/Social/Together/Music/EBook/Calendar/Sports 76.1% → 85.9%, whole app 70.9% → 75.9%. Offline smoke 566.
23:15 (09-24): Review 24 (AVPlayer subtitles + PiP, fresh context; no compile errors found by hand): re-picking the embedded subtitle track that is already on keeps the reported timeline (only a real option change clears it), the isPictureInPicturePossible observer fires on registration too, closing the PiP window while Harbor is in the background pauses, a failed/timed-out PiP start leaves focus where it was, the chrome's auto-hide no longer steals focus from the PiP placard, and error/connecting/no-audio cards, the leave confirm and the Together room stay out from under the placard. Left: a natural end during PiP still closes the player (and the PiP window); a stop during AVKit's start can race (no UI path reaches it).
23:30 (09-24): Review 25 (Spotify batch 4, fresh context; no compile errors found by hand): closing the Spotify setup sheet opened by "Reconnect for permission" refreshes the library page (cache + account) and the Add to playlist sheet (a re-sign-in keeps `connected` true, so onChange never fired), appended playlist pages drop ids already shown (ForEach needs unique ids), creating a playlist in the destination moves the next page's offset by one, and the permission text no longer shows twice.
23:45 (09-24): Kid player UI (subagent; most of the kid player — TransportKids, kid resume prompt, kids switcher, kid subtitle look — was already in PlayerKids.swift from the player batch): the kid branch of cinematic-player-loader. `KidsPlayerLoader` covers a kid's stage until the stream plays: the deep-sea background with the title art blurred at 20 %, the sea veil, bubbles, octopuses and the orange star; the pulsing logo or title, the "S · E" line, and "Connecting" / "Loading" or the TV torrent engine's readout in white without the large-file warning; Cancel (and Back) closes with no leave dialog. It replaces bp-connecting for kids, holds the focus ring while up, and the kids transport stays hidden under it. TransportKids' top-right slot shows FullscreenClock variant "kids" (`SettingsBridge.Slice` reads `fullscreenClock*`; format, seconds, the four styles, Ends at). PiP stays adult-only (TransportKids has no PiP control). Not ported: the picker's kid auto-play screen, "Try again" for a kid's peerless torrent. Offline smoke 566.
23:55 (09-24): Kid player follow-ups (subagent): the picker's auto-play screen. While instant play finds and starts a source, `PickerAutoStep` (App/Sources/Streams) stands in for the list: BpAutoStep for adults (bp-stream-steps.tsx: blurred art, logo or title, S · E line, Connecting / Trying source n, Choose a source instead, Back cancels auto; it replaces the inline "Finding the best source…" banner, closing parity-audit DT-1) and auto-play-transition.tsx's kid branch for a kid profile (deep-sea plate with bubbles, octopuses and star, pulsing logo, P2P · Searching sources… after 6 s / Stream is taking a while after 25 s while the TV engine fetches a torrent, "This source is slow. Try another.", Cancel → Choose another source). The kid loader's no-peers state (cinematic-player-loader.tsx): `TorrentReadout.onNoPeers` swaps the readout and Cancel for No peers found, the note, Go back and Try again (reloads like the connecting card). Not ported: the stub-source notice (no dead-streams port), Kitsu split-season numbering on the step. Offline smoke 566.
00:10 (09-25): Translation coverage 4 (subagent): `tools/l10n_coverage.mjs` counts Music's MusicCopy lookups by upstream id key (translated only when engine/music.ts COPY_KEYS lists the key and the catalog has it or its English) and stops counting data (user agents, identifiers, .glsl names, URL placeholders, player log lines, T() value args). MusicCopy passes an English result through T(), so the 20 music.* keys no upstream language defines come from `tools/locales-tvos.json`. Music/player String-typed copy through T() (Turn off, Anime4K panel, Loading…, subtitle Off/In sync/No subtitles/Best match/Added, audio Default/Track, mpv track fallbacks); interpolated copy on format keys. 35 new Apple TV strings in 15 languages (some from upstream's own translations, the rest by Claude, not native-reviewed). Coverage (de): Music 81.7% → 96.8%, Player 85.2% → 94.7%, whole app 77.8% → 79.8%. Offline smoke 566.
00:15 (09-25): Parity audit 2026-09-24 (subagent, read-only) saved as docs/parity-audit-2026-09-24.md: top gaps are dead player settings (skip intro/outro, still watching, track memory/rules), Home extra rows, per-episode watched marks + spoilers, Addons manager, video Now Playing, home-server preference, picker filters, sleep timer; six subagents took items 1–4, 6–11, 14.
00:30 (09-25): Per-show track memory and track rules (audit item 3, subagent): engine `player.trackPlan` runs lib/player-prefs.ts, lib/subtitles/subtitle-memory.ts and use-track-autoload's track choice (copied verbatim from the hook: trackBlockWords default "commentary", subtitlesOffByDefault, preferEmbeddedSubs, forcedSubsWhenNativeAudio, secondarySubLang auto second subtitle; the show's remembered audio/subtitle language, off and delay; the episode's remembered subtitle, re-adding an added one for the same release). `player.rememberAudio/rememberSubtitle/rememberSubDelay/noteSubtitleSource` are written from the Audio and Subtitles dialogs and the kid toggle. MPVPlayerController and NativePlayerController apply the plan on file load (`App/Sources/Player/TrackMemory.swift`), the old language match as fallback. `harbor.player.prefs.v1` / `harbor.subtitle.memory.v1` durable. Speed memory skipped (no rate control). Needs device: carry-over across episodes on both engines, off stays off, commentary skipped, added subtitle restored. Bundle 4071 KB, offline smoke 581.
00:45 (09-25): Episode watched state + spoiler masking (audit 4, 6; subagent): new `engine/episodeWatched.ts` (watched = Harbor manual marks + the Stremio library watched list merged as stremio-watched-pull does + this title's Trakt/Simkl history, a manual unmark wins; marks written to the manual store, Simkl, AniList/MAL for anime seasons, and the Stremio library through a new fflate `encodeWatchedField` since JavaScriptCore has no CompressionStream; `spoilerMaskFor` per strip card with next-up exempt via spoilerSkipNext; up-next mask). Detail strip: hold-Select menu (Mark as watched / unwatched / up to here / season), blur on still/title/description lifted on focus, showEpisodeRating/showEpisodeDescription (new two-line description, taller cards); the player's up-next drops a masked title. Needs device: the menu, focus blur, Stremio/Simkl/AniList writes. Bundle 4113 KB, offline smoke 603.
00:50 (09-25): Home extra rows (audit 2; subagent): `engine/homeExtras.ts` ports use-bp-extra-rows into rooms.home. Before the catalog rows: homeRows.listRows lists, home collection rows (cap 6), pinned rows (addon catalogs, AniList Trending/Top 100/list rails, MAL rails), Arabic/Russian rows (ar/ru UI + TMDB key). After: Favorites, My Watchlist, Trakt rails, Simkl rails (simklHomeRailsEnabled/UpNext/Trending + granular filters), Letterboxd rails, anime rows (unless hideContent.anime or classic). Dedup by key, applyHomeRowCustomization, 60-row cap. Slow rows get a 1.5 s grace; later ones raise `harbor:home-updated` and BrowseModel re-reads (catalog rows reused). **Behaviour change:** Home rows no longer strip anime for animeOnlyInAnimeRoom (upstream applies it to Continue Watching only). Settings → Home rows (`HomeRowsPanel.swift` over lib/home-customization): lists on Home, Simkl rail switches, reorder/hide/rename/numerals/reset. Skipped: animeFavoriteGenres (top-picks engine), cwAdvanceNext/animeCwEnd (use-cw-advance). Needs device: late row arrival and focus on re-read, Trakt/Simkl rails, ar/ru rows, the panel. Bundle 4175 KB, offline smoke 627.
01:00 (09-25): Picker leftovers + home-server preference + dead TV settings (audit 10, 11; subagent): saved stream filters (customStreamFilters/activeStreamFilterId) narrow the picker through engine tags (upstream's matchesCustomFilter), with upstream's fallback banner and a chip that cycles and saves the active filter; rows honour fullStreamDescription/pickerShowFilename with pictographs stripped; `ScoredStream` now decodes addonUrl/nativeIdx ("Sort: addon order" was a no-op). Play applies playbackSourcePreference (applyPreference from the hero Play / episode strip / handed-over Play): only "online" instant-plays; "home-server" opens on a Media servers chip and auto-plays the preferred server's copy (`homeServers.preferredSource` ports decidePlaybackSource; no copy → all sources). Player Quality panel for Plex/Jellyfin/Emby playback (`HomeServerQualityPanel.swift` → switchMediaServerQuality, swapped in place). Settings: controller navigation / Big Picture autostart / hide watched hidden on TV; hwdec Auto/Off drives mpv (videotoolbox/no; previews and tiles stay videotoolbox); posterQuality sizes poster art (img-size.ts); the play-button preference keeps load.ts's migration flags (upstream reverts a first change otherwise — upstream desktop bug). Needs device: filter chip, Plex/Jellyfin auto-play + quality switch, hwdec Off, poster sharpness. Bundle 4187 KB, offline smoke 649.
01:10 (09-25): Player parity (audit 1, 7, 14, 9; subagent): skip-pill-container settings (autoSkipIntro/Recap/Outro/Ad once per segment by upstream's positional rule, host-only in a Together room, a TV guard that waits for playing + a second tick so a pre-resume position can't jump; showSkipButton; skipButtonHideSec; the pill's ✕; the up-next card follows the displayed outro pill), "Still watching?" (use-still-watching: app-wide count of auto-advanced episodes reset by any press, 45 s Stop countdown, Back = Stop), sleep timer + playback speed in a Speed & sleep dialog (`PlayerTimers.swift`: 30 min / 1 hr / custom pause; End of episode / next episode stop at the end and skip auto-advance — **deliberate deviation**, upstream only pauses; 0.75–2× + custom, Set as default; defaultPlaybackSpeed on load via new `setRate` on both engines), and video Now Playing + remote commands (`VideoNowPlaying.swift`: MPNowPlayingInfoCenter/MPRemoteCommandCenter while a player is up, per-player claim, 350 ms double-press gate; MusicPlayer stands down and restores on close). Deferred: autoNextStreamOnStall (needs an auto-fired flag + dead-stream marking through Detail/picker), per-show speed memory. Needs device: AVKit vs our Now Playing on the native engine, the Play/Pause gate, music restore, pill ✕ / prompt focus, speed on both engines. Offline smoke 649.
01:20 (09-25): Review 26 (track memory, fresh context; no compile errors found by hand): both controllers count audio/subtitle selections, so a track plan that lands after the viewer already chose leaves that choice alone (upstream's userPicked), and a remembered added subtitle that finishes downloading after a choice is dropped (subRestoreAddRef); AVPlayer turns subtitles off for the plan only when it says off or nothing sideloaded is on; no automatic second subtitle for kid profiles or Multiview tiles; after an in-place stream switch the subtitle memory no longer keys on the original release's filename. Note: preferEmbeddedSubs has no effect as ported (external tracks are never auto-selection candidates in trackPlan).
01:30 (09-25): Review 27 (watched marks + Home rows, fresh context; no compile errors; the Stremio bitfield encoder was checked byte-for-byte against a real sample): a pinned AniList/MAL list rail with a failing first load no longer loops (fetchMediaListCollection resolves [] on failure, so "failed" now means "still no cache", a failed load waits a minute before retrying, and Home re-reads only when a list actually arrived); the Stremio library pull only decodes against a Cinemeta-aligned video list, and a bitfield whose anchor video isn't in the list decodes empty (stremio-core) instead of guessing an offset; a TMDB-sourced series reloads its watched state once the IMDb id resolves (Trakt history). Left: Search suggestions use the full Home rows (as upstream BP) and can trigger a Home re-read; an anime season mixing source ids is only partly marked.
01:40 (09-25): Addons manager parity (audit 8; subagent): `engine/addonsManager.ts` (`addonsManager.*`) ports views/addons.tsx and views/addons/*: the catalog merge, stremio-addons.net Browse (categories, Top rated / Top rising / Just added, search, paging, rising and New badges), Discover spotlight and community rail, the detail page (manifest facts, stars, documentation, related/recommended), install / configure / Install default / uninstall, install-link reading (new, update, re-configure replaces the old entry), Organize over reorder.ts (account order with validate / drift check / backup / read-back, device order via reorderInstalled, backups, restore, move all to account) and the age check with upstream's own question banks. Swift (`App/Sources/Addons/`): AddonsView rewritten (Discover / Browse / Installed, search, Add from URL, Adult chip), AddonDetailView, AddonConfigureView (setup page as a QR for the phone, install link back by phone typing or keyboard), AddonOrganizeView (Select picks a row up, Up/Down move it, plus up/down/top buttons), AgeGateView; `showAdultAddons`. Install default installs the published manifest (upstream's button does nothing) and is hidden when configuration is required. Not ported: Discover promo cards, Browse filter collapse, clipboard copy (QR instead), deep links still install directly. Needs device: Organize grab focus, the configure round trip, age-gate focus, live stremio-addons.net + account reorder. Bundle 4288 KB, offline smoke 684.
01:45 (09-25): Review 28 (player parity, fresh context; no compile errors on the merged head): the hidden AVPlayerViewController no longer writes its own Now Playing or handles play/pause (`updatesNowPlayingInfoCenter = false`, so no double toggle), a player that closed while its prefs loaded doesn't claim Now Playing back, a close that doesn't auto-advance resets the Still watching run, the sleep timer pauses a Watch Together room for everyone, the Quality chip has its own icon. Left: the Still watching countdown waits while PiP is on.
01:50 (09-25): Review 29 (picker/settings, fresh context; no compile errors): closing the player always tells the home server to stop, so a transcode started by a quality switch from direct play no longer keeps running (the engine stops the held session, or nothing when back on direct play); instant play only picks from the saved filter's pool (use-bp-stream-play); a quality switch made while paused starts the new stream paused; the row headline uses the engine's cleaned displayTitle. Note: customStreamFilters/activeStreamFilterId are in no sync section upstream and the TV has no filter editor, so the filter chip only shows for filters already in local settings. Left: offline preferred home servers aren't health-checked before auto-play; cachedOnly is applied after the saved filter (upstream: before, with its own fallback); the "Local Library" play-button choice does nothing on the TV.
02:00 (09-25): Review 30 (Addons manager, fresh context; no compile errors; account reorder verified: always a permutation of the loaded list, validated, drift-checked, backed up, read back): every new Organize visit backs up before its first account write again (upstream's per-mount backedUpRef; the engine had kept the flag for the app session), changing Browse filters mid-load no longer leaves an empty list, the age gate now also covers Top rising (both the official 24 h list — isAdultAddon instead of behaviorHints only — and the velocity fallback, which upstream leaves ungated), the configure success card's 2 s auto-close can't close a newer setup screen, and a manifest catalog without a type (or a numeric version) no longer fails the addon page's decode. Left: the Organize save label stays on "Checking"; Remove is offered for account addons like Cinemeta (as upstream).
02:10 (09-25): Stalled-stream skip + dead streams (audit leftover; subagent): `engine/deadStreams.ts` ports lib/dead-streams (markDead/isDead/flagStub/consumeStubEvent/clear, exported as `deadStreams`); `streamsRoom.deadRef`; `player.prefs` carries autoNextStreamOnStall(+Sec, stall-wait.ts clamp 5–120 s) and instantPlay. PlayPickerView hands `autoPicked` + `streamRef` over (StreamsModel.Resolved); DetailView/KidsDetailView thread `PlayerPickInfo` (autoPicked, attempt, streamRef) and `onPickAgain` into PlayerScreen. views/player.tsx: an auto pick that errors before playing, or (opt-in) hasn't started within the wait, is marked dead (4 h) and the picker reopens in auto mode skipping it (not live, not in a Together room, not after an in-place switch). use-stub-detection.ts under instantPlay: a file under 3 min is marked dead, its remembered pick forgotten, the picker returns (5 attempts, then close), with "Last source wasn't actually cached on your debrid yet." (Trying another. on the auto step / Pick another from the list. over the list, 6 s). **Deviations:** the ref carries the addon's URL so URL-only streams are skipped too (upstream's never matched); the adult auto step shows the notice too; no TV settings row for autoNextStreamOnStall (Big Picture has none; it syncs from desktop). Needs device: stall skip on both engines, error → next source, picker reopen timing, stub notice, kids flow. Bundle 4294 KB, offline smoke 701.
02:20 (09-25): Anime named seasons and episode orders (audit 5; subagent; bp-anime-seasons / bp-anime-season-chip): new `engine/animeSeasons.ts` ports use-anime-tvdb-panel (Kitsu → TVDB series, order types with episodes, active type from `tvdbSeasonType`, TVDB seasons matched onto the Kitsu list, TVDB-only episodes, Extras), falls back to upstream's `buildAnimeOrder`, and ports use-anime-preferred-season, `intentSeasonKey`, `seasonYears`, `shortOrderLabel` (`animeDetail.seasons` / `seasonsFor`, `animeSeasons.*`). `episodeWatched.state` takes the shown chip's keys and `mark` gains a "shown" scope (markMany, grouped by each episode's source id — closes review 27's mixed-owner season note). The anime detail draws the Episodes heading, named season chips with year spans and episode counts (extras behind a divider) and the Order chips in one focus section; cards get S/E tags and "Abs E"; the order toggle writes `tvdbSeasonType`; the old Kitsu-season grouping stays as the fallback. Not done: AniList/MAL watched sets for the opening season, franchise pool, TVDB episode images, per-episode ratings, Filler badge; auto-advance still follows the Kitsu order. Needs device: chips/years, D-pad hero → chips → strip, order switch persistence, a TVDB-only card, season marks per chip, no-mapping fallback. Bundle 4318 KB, offline smoke 726.
02:30 (09-25): Continue Watching advance + anime Top Picks (audit Lower: cwAdvanceNext, cwHideCaughtUp, animeCwEnd, animeFavoriteGenres; subagent): `engine/cwAdvance.ts` ports use-cw-advance.ts into rooms.continueWatchingWithExtras and the Anime room CW (a finished episode moves to the next aired one as Up Next; a caught-up show leaves the row or, with animeCwEnd timer, shows a countdown and a re-run timer; the past-the-last-episode guard, anime split-franchise remap, episodeHiding, resurfacing, Trakt/Simkl/AniList/manual watched sources; a 1.5 s Home / 0.8 s Anime grace returns the unadvanced row and a changed late result raises the room's re-read event). `engine/animeTopPicks.ts` ports use-anime-top-picks + use-watch-history-recs (recs, sequels, taste + favourite genres, new/airing/top pages, all on Jikan's throttled queue, upstream's cache keys) as the Anime room's first row, "Top Picks for You". Deviation: when every pick is filtered, the TV falls back to the top-airing row (no hosted hero list). Settings: Home rows panel gains the Continue Watching group; Anime rows panel gains Tune anime (genres, origins, hide watched). Needs device: Up Next / countdown cards, the late re-read focus, the picks row landing, both panels. Bundle 4371 KB, offline smoke 757.
02:40 (09-25): Review 31 (stall skip + anime seasons, fresh context): the error/stall skips are capped like the stub path (MAX_AUTORETRY_ATTEMPTS 5; a stream with no ref can't loop) — past the cap the stream is still marked but the viewer keeps the error/connecting card; the stall wait restarts on every reload (Try again, the switch to mpv); the anime order chip shows the order the engine answers with (not set ahead of the reload), an empty reply keeps the current order and chips, and a stale reply is ignored; "Mark season as watched" on a chip uses upstream's airedOnly (undated episodes after the last aired stay unmarked). Left: animeDetail's 8-page cache can make the order toggle a no-op on a deep stack; focus when the TVDB chips replace the Kitsu season buttons.
02:50 (09-25): Review 32 (CW advance + Top Picks, fresh context; no compile errors; the cwAdvance port checked line by line against use-cw-advance.ts): a pass that outlives the grace keeps the previous advanced/removed state applied (as upstream) instead of returning the raw row, so hidden caught-up shows don't flash back after playback, and late answers re-read only when what shows changes; air timers stop when cwAdvanceNext is turned off and on a profile switch; Top Picks runs the watch-history recs alongside the Jikan pages (upstream runs them in parallel) and re-seeds its rotation on a new day. Compile-risk sweep of every Swift change since run 161 (27 files, ~3,750 lines: argument order at every changed call site, tvOS availability, protocol/switch completeness, duplicates, access, Codable, concurrency) found nothing more. Left: fetchEpisodeList's session caches hold a newly announced episode until relaunch; a superseded CW pass keeps fetching; the anime "hide watched" filter reads local flags only (pre-existing); genre toggles each trigger a Top Picks rebuild; CW can land up to ~7.5 s late on a cold start.
03:00 (09-25): AI search (audit Lower; subagent): `engine/aiSearch.ts` ports lib/ai-search.ts + use-ai-suggest + the desktop AI search settings (OpenRouter/Groq, models trimmed to the live lists, custom id, Jina web context). Keys are typed on the phone and kept in the Keychain under `harbor.ai-search.keys.v1.<scope>` (added to KeyValueStore.secretPrefixes), never in the settings blob (a key left there moves over once); Swift only sees the last four characters. Search screen: an AI search button (hold for models), example hint, auto-run after 1.6 s or phone Go, thinking state, retry card, the AI picks list; an episode pick opens its detail on that episode (`DetailView` episodeHint). Settings › Setup gets an "AI search" row opening `AISearchPanel` (Big Picture has none; mirrors the desktop section). Needs device: phone typing into the secret field, hold-Select model menu, real OpenRouter + Groq searches, Keychain persistence.
03:05 (09-25): X-Ray on pause (audit Lower; subagent; components/player/xray, lib/xray/use-xray-cast.ts): `engine/xray.ts` loads the cast from TMDB details, else TVDB, and builds the side list, the Cast/Crew/About browser and status copy; `PlayerXRay.swift`: the cast list opens when the viewer pauses with the chrome up (its ✕ leaves the X-Ray button), "View all" opens the full-screen browser, cards open PersonView inside the player (new `onClose:`, never a cover over the player), trailers inline. No face matching on the TV, so no "In scene" tab. Settings › Playback gets an "X-Ray" row (off by default; Big Picture has none). Hidden during PiP, panels, prompts, the Together room; kids get it as upstream when their setting is on. Needs device: focus into the list/browser, Menu/Play-Pause handling, the embedded person page. Bundle 4407 KB, offline smoke 799.
03:15 (09-25): Harbor Voyages (audit Lower; subagent): upstream's `lib/voyage` store runs in the engine (`engine/voyage.ts` → `voyageRoom.*`; `discoverRoom.buildFor` returns `voyagePool`). Big Picture doesn't expose Voyages (the banner is data-tv-skip), so the TV puts it where the desktop does: a Voyages banner on the Discover room after Genres, opening a full-screen VoyageView (theme chooser with 3/5/7 length, queue rail, 1-of-3 picker with a focused-poster card of cast and director, ready / exhausted / complete / up-next panels, streak chip). Films open DetailView over the voyage (movies autoplay) and progress refreshes on return. Skipped: voyage-launch (lottie boat), voyage-prefetch (desktop picker cache). Upstream quirks kept: the banner's watched count reads playedIds (always 0 for new voyages); TMDB recs only feed Uncharted Waters. Needs device: the banner scroll, focus when the three headings re-rank, autoplay over the voyage. Bundle 4433 KB, offline smoke 822.
03:20 (09-25): Review 33 (Voyages, fresh context; no compile errors, testDiscoverRoom unaffected): a pick moves focus as soon as the pick shows and the TMDB refinement lands after (no jump back to heading 1 a second later), the banner marquee stops when it leaves the screen and follows Reduce Motion live, a failed credits call isn't cached as "no credits". Left: with exactly two banner items the strip shows a gap before it loops (as upstream CSS).
03:30 (09-25): Review 34 (AI search + X-Ray, fresh context; no compile errors; keys verified Keychain-only, not synced, never logged, shown masked; model output only picks titles): deleting a profile (locally or by sync) also drops its AI search keys (`harbor.ai-search.keys.v1.` in both PROFILE_KEY_PREFIXES lists); moving a leftover key out of the settings also strips it from the shared and mirror blobs (forkToProfile would copy them into UserDefaults); X-Ray only keeps a full answer in its Swift cache (a failed TMDB lookup or a missing key is asked again); phone Go runs AI search on the query just typed; no OpenRouter catalog fetch without a key; **X-Ray is not shown to kid profiles** (deliberate deviation: its person pages show unfiltered filmographies). Left: an AI episode pick's strip still scrolls to the resume episode and the Play bar shows the resume point's progress; unlinking a profile doesn't carry its AI keys over (upstream's fork copies them).
03:45 (09-25): Review leftovers 29/31–34 + per-show speed (subagent): an AI search episode pick scrolls the strip to that episode (before the resume point; the scroll also re-fires on a season change) and the Play progress bar hides when the pick is another episode; `animeDetail.seasons` takes the page's meta and reloads a page that fell out of the 8-page cache (the order toggle works on deep stacks); a Kitsu season picked before the TVDB chips seeds the chip pick and focus moves to that chip; "Hide anime I've already watched" reads the Simkl/AniList maps (use-bp-anime-watched isAnimeWatched) over CW + hero/picks ids; Top Picks rebuilds 1 s after the last genre toggle; the TV's Play button behavior has no Local Library option (a synced "local" reads as Ask); a preferred home server that is offline or silent for 5 s (lib/media-server/health) never auto-plays and a hand pick wins; the Speed & sleep rate is remembered per show (player-prefs rate) and applied on load before defaultPlaybackSpeed; the Voyages marquee repeats short lists so two items loop without a gap. Needs device: chip focus hand-off, strip landing on the pick, marquee with 2 items, speed carry-over. Bundle 4445 KB, offline smoke 838.
03:55 (09-25): Review 35 (leftovers batch, fresh context; no compile errors): a Kitsu season pressed while the TVDB request was out now opens its chip (the pick is read again after the reply); the preferred home server's health uses the probe's own outcome, so a server that was active earlier and now times out counts as offline instead of auto-playing; kid profiles always start at 1× (per-show rate memory is keyed by show, and the kids transport has no speed control); the Kitsu→chip focus hand-off only fires in the same update (0.1 s), not after a move away. Left: the Anime room's late-id set is shared across profiles and stops growing at 400.
04:10 (09-25): Browsing Harbor during Picture in Picture (subagent; upstream use-pip-mode / requestPiP leave the app usable while the picture floats): when PiP starts on the AVPlayer engine the player screen steps aside instead of closing. `Player/PiPBrowse.swift` raises a browse layer — a second Big Picture shell on its own AppModel in a window above the app's own (below ShellOverlay's lock and saver) — while PlayerScreen keeps playing under it, so progress saves, scrobbles, Now Playing, torrent ownership, the Together room and the sleep timer carry on. The PiP window's restore button lowers the layer before AVKit brings the picture back (restoreUserInterfaceForPictureInPictureStop); Back on the layer's Home returns to the player's placard, still in PiP (new "Browse Harbor" chip); closing the PiP window stops and saves while the viewer keeps browsing; a video or Multiview opened from the layer stops the PiP film first; a natural end that advances (or asks Still watching) lowers the layer first; a profile switch ends it; Play/Pause on the layer toggles the film. PlaybackState tracks per-player claims (claim/release: PlayerScreen and Multiview) so overlapping players never clear each other; live previews stay off under the layer; mpv display criteria follow the player's own window; Multiview's full-screen player keeps the placard (`browseDuringPiP: false`). Not done: PiP browsing for a player opened from the layer (placard only), deep links while the layer is up, added subtitles inside the PiP window. **Riskiest change of the session** — needs device: focus/key between the two windows, restore vs close detection, the save timer and Play/Pause while browsing, the PiP-to-new-video hand-off, HDR/DV matching from the layer, memory with two shells.
04:20 (09-25): Stage 12 music volume (subagent): upstream's music volume (music-dock.tsx mute + "Music volume" slider, player.ts setMusicVolume, `harbor.music.volume.v1`, default 0.82) on Now Playing as a mute button and − / + buttons (5 % steps; tvOS has no slider) with "Music volume · NN%"; MusicPlayer applies the level to the AVQueuePlayer as its cube (mpv's cubic curve) and to Spotify through the existing `harbor_spotify_set_volume`; `harbor_spotify_play` starts each track at the level instead of 1.0; the key is durable. **Behaviour change:** music now starts at upstream's 0.82 default (about 5 dB quieter than before for non-Spotify sources). Spotify Connect is not in upstream (no librespot-connect / Spirc / zeroconf in src-tauri), so nothing to port; docs/music-spec.md says what it would take. No Rust or header changes. Needs device: Now Playing focus order, remote stepping, level after relaunch, Spotify changes heard within ~0.5 s. Bundle 4445 KB, offline smoke 839.
04:30 (09-25): Review 36 (PiP browse layer, fresh context; no compile errors; the layer can't get stuck — Menu at its Home returns to the player): the main shell's default-focus and shoulder-tab hooks go to PiPBrowse's stash while the layer is up (a player cover closing under the layer re-appeared the main shell, which took the hooks over the layer's, and lowering restored a nil onTab); only the visible shell hosts Watch Together invites (the hidden main shell would auto-join a film into a window nobody sees); Multiview releases its playback claim on any disappear (a teardown with its full player up left an orphaned claim: music paused, previews off, no screensaver until relaunch); `PlaybackState.active` is read-only outside the claims; "Browse Harbor" translated (15 languages). Left: deep links while the layer is up go to the hidden main model; the layer's AppModel repeats profile-change loads; the layer's language/theme freeze until it goes down; UI sounds muted in the layer while the PiP film plays.
04:40 (09-25): PiP browse layer, review-36 leftovers (subagent): deep links while the layer is up open in the layer (`RootView` onOpenURL → `PiPBrowse.layerApp`); the layer's model is `AppModel(isBrowseLayer: true)` so a profile switch reloads settings/theme/Together once; `PiPBrowseRoot` keys its own tree on theme revision + language (a language change applies at once; held only while a player opened from the layer runs, `PiPBrowse.filmInPiP`; a theme change still waits in ThemeStore until the PiP film stops); UI sounds play in the layer while the only film is the one in the PiP window (upstream's lib/sfx.ts never mutes for video), still muted over a player opened from the layer and on the placard.
04:45 (09-25): Review 37 (music volume, fresh context; no compile errors, C signatures match): a viewer who listened before the level existed (recents or liked tracks saved) keeps full volume instead of dropping to 0.82 (about −5 dB streams, −11 dB Spotify through librespot's log mapping); new listeners get upstream's 0.82; the Now Playing art is 280 (was 340) so the volume row doesn't push the transport off screen; − / + have distinct accessibility labels.
04:50 (09-25): Kids Play Zone arcade (subagent): upstream's "Games" (kids/play/games.tsx) is a catalog of 53 third-party Scratch projects shown in a web frame of scratch.mit.edu/projects/<id>/embed — Harbor has no game code there, and tvOS has no web view and can't run Scratch's player, so the games can't run on the TV. New `App/Sources/Kids/KidsGameArcade.swift` gives the TV upstream's fourth Play Zone card with the same catalog (upstream order, 7 category buttons, Scratch thumbnails, 4-column grid) and a hand-off panel per game: thumbnail, "on Scratch" label and a QR code for the same embed page to play on a phone, tablet or computer (PLAN decision 7). Back closes the game, then the activity, then the Play Zone; focus returns to the game's card. Match the Pals, Bubble Numbers and Ocean Wonders were already native. Needs device: focus into/out of the panel, grid scrolling, thumbnails, QR scan.
05:00 (09-25): Review 38 (Play Zone arcade + PiP leftovers + review 37, fresh context; no compile errors; the QR hand-off only encodes the Scratch embed page and nothing on the TV navigates elsewhere): the music volume migration is one-shot (the first read stores 1.0 for an existing listener or 0.82 for a new one, so a new listener's own recents can't lift them to full on the next launch); the arcade stays mounted under an open game so focus returns to the game's card even deep in the grid; QR codes are made once per game. Note: on the phone the Scratch embed opens as a normal page (upstream's iframe sandbox doesn't apply there).
2026-09-25 00:25 UTC: Upstream re-sync to beta-branch 770ca0bd (music Now Playing up-next suggestions; the Music bug pass ports it). Player bug pass (subagent): **mpv never reported a natural end** — with keep-open=yes END_FILE(eof) never arrives, so on mpv next-episode, Still watching, the sleep timer's end-of-episode and the watched-at-end save were all dead; the poll now reads eof-reached like upstream's mpv.ts, once per file, gated by isNaturalEnd (unknown duration or ≥85 %), live excluded. A seek made before FILE_LOADED is held and applied on load (the resume prompt answered during a slow connect started at 0). MPVPlayerController touches `status` on main only (a queue/main data race). The Metal layer is kept alive until mpv_terminate_destroy returns. PlayerScreen re-owns the switched torrent on re-appear. The source error card's Try again resets the connecting clock and the stale controller. Synced custom speeds outside 0.25–4× are ignored (a 0 would stall AVPlayer). A second player pass is checking the rest (AVPlayer seek before ready, live reload on EOF, AVPlayer natural-end rule, stall-skip start detection, Anime4K storage).
2026-09-25 00:29 UTC: Music bug pass + up-next (subagent): Now Playing's Up next offers the current track's radio when nothing follows it (upstream 770ca0bd up-next.ts: engine `music.upNext`, cached per track, "Building up next" while loading, a pick plays with [current, …suggestions]). Fixes: Play after a stream queue ended did nothing (AVQueuePlayer had dropped the item; it now restarts the entry as player.ts does); the 0.5 s tick reads only the item bound to the current entry (a gapless hand-off put the next track's length on this one and on its scrobble threshold); one Play/Pause per 350 ms across the screens' onPlayPauseCommand and the remote commands; remove(at:) shifts the bound item and Spotify indices; the progress stamp refuses lengths Int() would trap on; concurrent Spotify Web API calls share one token refresh (a rotated refresh token was spent twice and the loser wiped the saved sign-in — upstream tokens.rs has the same race). No Rust defects found. Bundle 4441 KB, offline smoke 843.
2026-09-25 00:32 UTC: Bug pass — shell, profiles, storage, engine host, eBook, torrent, phone (subagent): one per-profile purge list with all of upstream's 33 prefixes serves both profile delete and profile-list drops (the short copies left Simkl/AniList/MAL sessions, history, watchlist and addons behind); Prefs enforces its 400 KB budget (a value ≥1 KB over it goes to Caches) and KeyValueStore no longer caches a stale read over a concurrent write; the screensaver wakes on any press (it could pop up right after Who's watching) and LB/RB wake it instead of turning tabs behind it; `HarborOverlayWindow.mainWindow` prefers normal-level windows; a cached EPUB that no longer parses is fetched again and parsed books keep only their reading-order documents (not every image); the Edge margin is clamped to 0…0.1 (a synced stray value broke the layout); the Sports tab follows the stored consent at launch and on decline; the PIN shake animates (Int → CGFloat); Who's watching tiles under the keypad are disabled; the TMDB key check reads logs past the full 200-line ring. Checked OK: JSC threading, the phone LAN server limits, torrent FFI, the EPUB zip reader, Multiview's 4 decoders, format specifiers in all 41,231 translations.
2026-09-25 00:32 UTC: Player bug pass 2 (subagent): AVPlayer holds a seek made before readyToPlay and plays from there once ready; AVPlayer's end follows playback-end.ts isNaturalEnd through a shared `PlaybackEnd` helper mpv uses too (a truncated stream no longer counts as watched or advances); a Watch Together guest never auto-advances (canChangeEpisode); live channels that run out reload as in use-auto-end-exit.ts (1 s, at most 5 in 15 s, then close) on both engines; the stall skip counts a pause as "started" only once the engine has the file open; mpv/native onReady ignore a controller replaced since creation; Anime4K shaders moved to Caches and re-download if purged; MPVMetalLayer.drawableSize can't trap on NaN/inf. Offline smoke 845.
2026-09-25 00:37 UTC: Browse/Detail/Streams/Search/Addons/Discover/Collections bug pass (subagent): addon metas with odd field types no longer fail a whole room (Meta has its own lenient decoder, in an extension so the memberwise init stays); duplicate list ids removed across rows, grids, cast, episodes, awards, Continue Watching and collections (`uniquedById()`); a cover closing no longer redoes first-load work (Detail with autoPlay reopened the picker after every player close — the viewer couldn't get back; the picker searched again and dropped the P2P "Stream" pick under its own dialogs; Queue deck / Collections / addon page / anime award resets); old results no longer overwrite newer ones (stream search generations, Search runs after edits, addon detail, per-season watched state); catalog and category switches mid-load no longer leave empty grids; Queue deck Skip removes the right card. Engine: a replaced stream search answers "aborted" and leaves the newer results (a pick could resolve the wrong stream), the Person collaborators fetch/refresh loop is gone (tight loop offline), anime late ids are per profile with oldest-first eviction. Bundle 4443 KB, offline smoke 849.
2026-09-25 00:41 UTC: Music bug pass 2 (subagent): the music's Now Playing hand-back after a video closes no longer depends on the order of `VideoNowPlaying.end()` and `PlaybackState.release()` (synchronous sink + an `onEnded` hook); a stream or Spotify failure mid-song scrobbles what was heard before recovering (engine.rs scrobbles on any EndFile; the retry starts at 0:00 as upstream's recoverPlayback does); a gapless preload no longer reports Jellyfin/Navidrome "now playing" or closes the audible track's Jellyfin session (reports held and sent on `music.started`); saved-sign-in Spotify restores are skipped offline or within 60 s of a failed one (a Spotify queue waited up to 45 s per entry); SpotifyAudioOutput reconnects mixer → output and source → mixer (44.1 kHz stereo) after AVAudioEngineConfigurationChange (needs an HDMI/AirPlay check); a Spotify token refresh landing after Disconnect is dropped. Bundle 4444 KB, offline smoke 851.
2026-09-25 00:47 UTC: Shell bug pass 2 — decode hardening (subagent): synced blobs decode one field at a time (shared `App/Sources/App/Lenient.swift`: LenientKey, LossyItem, c.lenient/c.lossyArray, used from extensions so memberwise inits survive), so one null or mistyped field no longer drops the whole settings slice (the app ran on defaults: TMDB key "missing", English UI), the whole profile roster (launch fell back to the old Prefs copy and roster reloads were ignored) or the whole Watch Together view; a nameless participant or draw event also made the engine's together `view()` throw inside the emit timer, freezing the room (smoke check). CacheStore hashes file names over 255 bytes (readable prefix + SHA256, key in a sidecar; existing files still read). TorrentEngine range-checks the file index before Int(). The Swift-only curfew record is purged on profile delete and roster drops. A Watch Together invite doesn't auto-join under the screensaver or curfew lock. The Trakt/Simkl device-code poll stops when its panel closes. A Together guest gets no up-next card / Play now (upstream hasNextEpDisplay). Offline smoke 852.
2026-09-25 00:59 UTC: Browse bug pass 2 (subagent): lists fed by synced or third-party data decode lossily through a new `LossyArray` property wrapper (App/Sources/Engine/LossyArray.swift), so one bad element no longer silently empties the row (Continue Watching, whose `LibraryItem` also decodes leniently; room/service/anime/letterboxd/discover/search/person/detail/kids rows; library sections; calendar cells; addon cards; collections). ImageLoader decodes downsampled ImageIO thumbnails at the size `RemoteImage` measures (1920 px long edge when no size is given), off the main thread, with the memory cache at 1/16 of RAM (48–160 MB, was 200 MB). Detail's local resume is one engine call (`player.localResumes`, newest first like upstream's lastPlayedEpisode) instead of one per episode. The picker checks resolved links (`PlayableURL`) and shows an error instead of silently doing nothing; the Kids in-player switcher too. addonsManager cleans community and account manifests and coerces every card to AddonCard's types (a numeric name threw and failed the whole Addons list; smoke check). Organize binds focus to the row key and scrolls after a moved row. Bundle 4448 KB, offline smoke 858.
2026-09-25 01:15 UTC: Watch Together / scrobbling / Settings bug pass (subagent): SettingsBridge reads and writes the active profile the way upstream lib/settings.tsx does (engine `settings.activate` / `settings.patchFor`), so the `harbor.settings` copy that upstream code reads follows profile switches and every TV-side change (Simkl "Scrobbling off", TMDB key, cinemetaEnabled were ignored), and a profile with its own settings but nothing saved no longer loads the defaults; only the newest settings load or patch applies (a late one restored the previous profile's settings and language). After the player starts closing, no scrobble or Watch Together tick runs (the closing "pause" became "start" again, and a host's heartbeat re-published the title it had just cleared). Watch Together: chat toasts follow the newest line (the 200-line cap had frozen the count); "ready" and the guest's initial sync reset when the room changes; the host re-announces the lobby once its source arrives. The Settings column keeps its depth across a language change; a Trakt/Simkl code fetched after the panel closed no longer polls. Known gap: a player torn down without finish() sends no final scrobble.
2026-09-25 01:15 UTC: Kids/Sports bug pass (subagent): the kid curfew day follows the local calendar (the ISO8601 style was GMT, so allowances reset at 8 pm EDT and parent unlocks expired mid-evening), and CurfewState publishes only on change (RootView re-rendered every second); the Kids page and kids detail no longer reload when a cover closes (paged rows snapped back to page 1, and the season jumped back to 1 after each episode); a late kid switcher resolve no longer swaps the video after it closed; Ocean Wonders ignores cancelled photo loads; the kids shell only disables itself while the parent PIN pad is up. Sports: failed hub feeds wait for use-hub's retry interval instead of reloading on every page read (a tight loop offline; smoke check); a live card press runs one watch scan at a time; Make it yours drops stale team lists and updates follow ticks in place (the grid blanked and lost focus); the addon panel ignores late stream answers after Back or close; the athlete page is pushed once; hero art resets per game; page dates use Harbor's UI language. Follow-up (01:18): the day strip's weekdays use Harbor's UI language (engine `sports.days(anchor, locale)`, "Today" translated in Swift; online smoke check) and SportsView stops its 60 s poll when covered (`.task` restarts it). Both passes merged: bundle 4450 KB, offline smoke 861.
2026-09-25 01:23 UTC: Lifecycle bug pass (subagent): the engine now hears background/foreground and network changes (`runtime.setVisibility` / `setOnline` → `document.visibilityState` + `visibilitychange`, `navigator.onLine` + `online`/`offline`; new `AppLifecycle.swift` with an 8 s background task). So profile sync pushes its queue when the app leaves the screen and pulls on return when stale, the Harbor session refreshes on wake, storage-recovery and Suwayomi progress save on hidden, Trakt/Simkl pending syncs retry when the network returns, and a MAL refresh that fails offline no longer signs MAL out (smoke checks). The engine is built off the main thread at boot (the splash froze for seconds) and `HarborEngine.loaded` never waits on a build (separate build lock). Boot waits at most 8 s for the first sync pull. A deep link that launches the app no longer jumps to the shell mid-boot. The screensaver's idle clock restarts when the app returns and never fires while it is not active (use-idle-screensaver.ts). KeyValueStore no longer keeps a second RAM copy of large (16 KB+) cached values once they are on disk. Noted, not fixed: a stored literal `null` in `harbor.reminders.v1` makes upstream reminders.ts throw at load (nothing writes it today). Bundle 4451 KB, offline smoke 867.
2026-09-25 01:36 UTC: eBook / X-Ray / Calendar / Wrapped / lists bug pass (subagent): a page inside a paragraph longer than a screen page reopened (and re-laid out after a text or width change) on that paragraph's first page — resume now lands on a TV page anchor the engine keeps next to upstream's line (`ebook.savePosition`/`openChapter`, key `…:harbor:tv-anchor`, which upstream's `:harbor` suffix reader skips; checked against the line and text identity; smoke checks), and relayouts keep the page's first character; a chapter's last page saves the last line, so finished books reach 100 % / "read"; a chapter move flushes and awaits the pending save (it was cancelled); late chapter failures and stale layouts no longer land; the reader stops narration and saves on disappear, not only on Close/Back; EPUB unzip skips images, fonts and audio (memory, and the 300 MB budget could cut off later chapters); ebook detail loads once; shelf counts are localized. X-Ray no longer spins forever when the engine call fails. Calendar keeps the fired-reminders banner across covers, and the day view, reminders manager and Wrapped keep focus after a title closes. Shared lists don't refetch on cover close. Library applies only the newest feed request. Search, AI search, Voyages and the Letterboxd panel were checked: no defects. Bundle 4452 KB, offline smoke 869.
2026-09-25 01:48 UTC: Live TV / IPTV and stream sources bug pass (subagent): gzipped XMLTV guides now load (the fetch took the lossy text path and wrecked the gzip header; now bytes via harborResponseType base64, 200 MB cap; smoke check). Guide parsing is linear: whole-text drainBlocks was quadratic (4 MB took 25 s; a 50 MB guide never finished and held the engine thread); gzip is inflated and decoded 1 MB at a time (smoke check: 30,000-programme .xml.gz with accented/CJK titles). Guide URLs follow use-epg/epg-store: the source's own, both Xtream addresses, then every "Guide data only" source, with fallback; guide-only sources are no longer pickable or used by the Home row. The active source is remembered (harbor.iptv.active; engine live.activeSource/setActiveSource). The guide grid rebuilds its lanes once the guide lands (they stayed "No program info") and fetches lanes only for rows on screen, 40 at a time, capped at 600 (was ~20 MB per extension on big lists). A late load for an earlier source no longer lands over the newer pick; the guide loads without holding Add, pick or Refresh; a closing cover no longer reloads Live TV. The in-player TV Guide asks now/next as rows appear (was the first 120 only). Playlist VOD's loading line survives a source switch. Picker: "Cached" is upstream's streamIsCached, so debrid links count as instant (engine tvCached); the sort follows settings.streamSort (default addon order, forced by ranked addons), is saved, and Harbor order uses bp-stream-filters' keys. Bundle 4459 KB, offline smoke 877.
2026-09-25 01:59 UTC: Onboarding / profiles / account / addons bug pass (subagent): profiles made on the TV now follow the primary's Stremio account like upstream stremioSourceProfileId (they had no library, watchlist or Stremio addons; Settings' connected list too; smoke check), and Stremio sign-in/out goes through KeyValueStore (still Keychain) + syncStorage, so the bundle's addon-store/watchlist/mark-watched stop using a stale key until relaunch; a profile deleting itself returns to Who's watching instead of landing in the PIN-locked primary; profile sync starts whenever a signed-in profile becomes active after boot (it stayed off all run when boot restored a profile without a session, or after Sign out); stremio:// manifest links open the install dialog (install-modal parity: confirm, update, replace a re-configure; waits for the adult shell) instead of installing silently for any profile; the install dialog re-reads an edited link; PINs must be 4 ASCII digits ("+123" locked a profile for good); Create can't make two profiles and the editor's late load no longer overwrites a typed name; a late notification poll no longer restores the previous account's badge; keys, emails, usernames and names trim newlines. Noted, not fixed: no watchlist aggregate rebuild at boot/profile switch (upstream WatchlistSync); the TMDB step offers "Save it anyway" for a rejected key too. Bundle 4459 KB, offline smoke 878.
2026-09-25 02:02 UTC: Player tracks and overlays bug pass (subagent). Subtitles: `subtitles.prepare` now asks the host for raw bytes, because the text bridge's lossy UTF-8 broke every zipped subtitle and every non-UTF-8 file. The TextDecoder shim adds WHATWG UTF-16 and the single-byte legacy encodings (`engine/shims/legacy-text.js`, generated from Node's tables), which upstream's encoding sniffing needs; Latin-1, Cyrillic, Arabic and UTF-16 SRTs were "decode-unhealthy" (upstream epub UTF-16 decoding works now too; smoke checks). A prepared subtitle is cached per URL and reused by Find more and the remembered-subtitle restore (no re-download on every open), and "Added" marks last for the session. mpv applies `sub-filter-sdh` (subHideSdh) as AVPlayer did. Skip segments read mpv's chapter list (upstream chapters.ts: Opening, Ending, Recap; new `chapters()` on PlayerEngineControlling), newest answer only. When a new engine takes over (live reload, AVPlayer-to-mpv fallback) while in PiP, the browse layer goes down and the PiP state is cleared instead of sticking. Anime4K reapplies after Try again, drops overtaken runs, and a second shader download request waits for the one running. mpv reloads keep the viewer's audio offset. The source error and connecting cards no longer pull focus behind an open dialog. Bundle 4486 KB, offline smoke 885.
2026-09-25 02:22 UTC: Music sources / manga bug pass (subagent): an empty music home (opened before the network was up) is no longer cached for six hours (catalog_commands.rs stores rows only when there are some; listeners with history saw only Recents with no retry; smoke check); a Plex probe that found no server is not kept for five minutes (session.rs stores only a discovered config; smoke check); the manga reader warms each page at the width it is drawn at (paged pages were decoded at 2400 px under a different cache key, about 32 MB each, never used), and a new turn, chapter change or close cancels the warm-up; a chapter change counts as loading at once (a quick second press marked the next chapter complete and skipped it); the manga detail keeps its language and range and doesn't refetch every chapter when the reader closes; the Manga room drops popular/tags answers from a source switched away from; the anime page's "Read the Manga" entry no longer vanishes (and loses focus) when the manga page closes; music album/artist/playlist pages load once (a child page closing refetched and dropped "Load more" albums). Checked against upstream's Rust, no change: Last.fm threshold/signing, Subsonic salt/token, per-start stream URL resolution. Bundle 4486 KB, offline smoke 887.
2026-09-25 02:23 UTC: Browse bug pass 3 (subagent; Home, Discover, Detail, Search, navigation): Continue Watching cards resume in one press when they can say what they would play (bp-cw-row bpCwResume; engine `_cw.anime` = isAnimeCwItem; smoke checks), otherwise they open Detail. Holding Select on a card opens the quick panel, and "Remove from Continue watching" now appears only there and closes the panel (cards had no way to be removed). Detail finds the Stremio library entry under the IMDb id TMDB resolved (use-bp-library-item candidates), so `tmdb:` pages show Resume and In Watchlist. Detail's resume follows bp-resume-mark: the named episode must be on the page (no more "Resume S3:E4" over a Play of the season's first episode), a finished episode with no position still resumes, and a stale point clears on reload. The season no longer snaps back to the resume season each time a cover closes. The up-next card and auto-advance only offer an aired next episode (views/player.tsx airedNext). The Discover award page shows the winning work (`workTitle`; it read a `title` that never comes; smoke check) and says so when it can't load. Search with an empty field no longer shows a focused suggestion as "Top match". A late anime hero meta line or Home band mark no longer replaces the current one. Open: one-press resume waits for the whole Detail load (upstream waits at most 500 ms). Needs a device check: the CW long press, focus after a remove. Offline smoke 890.
2026-09-25 02:33 UTC: One-press resume no longer waits for the whole Detail load (bp-detail pending play, bp-play-request; subagent): DetailModel publishes `loadStage` (meta → resume → library, forward only) and `knowsPlayTarget`, and DetailView opens the picker the first time it holds, still only once (`autoPlayFired`). Movies, Continue Watching episodes on the page and Watch Together room episodes fire right after the meta fetch; series with no named episode fire after the resume read (tt ids, anime), or after the resume read under the IMDb id TMDB found (non-tt ids). Episode art, TMDB extras, facts, awards and trackers keep loading under the picker. No 500 ms cap for an unnamed series (avoids upstream's S1E1 resume overwrite). The start position still comes from the player's own resume read, not the page. Needs a device check: the picker opening while the first load is still running.
2026-09-25 03:00 UTC: Performance pass (subagent): playback saves locally every 4 s as upstream says (were every ~2 s) and flush on pause and on leaving the app; the Stremio library entry is written on use-stremio-sync's 30 s tick or a flush (was a GET + PUT per save; smoke check); saves and rememberPlayback no longer re-encode the whole meta (episode list included) through AnyJSON on the main thread. Live TV asks now/next for the first 60 rows and then for rows as they appear, and the 30 s tick re-asks only those (was every channel of the category: ~2.7 MB of JSON per tick on a 6,000-channel list); the guide no longer redraws every scroll frame. Awards catalog: one install at a time, read off main. Room/Kids shelf caches are read and written off the main thread, and an identical reload publishes nothing. CardMarksStore, SettingsBridge and SyncReader publish only on change; bundled avatars are cached; ISO date formatters are made once. Noted, not fixed: the ambient mosaic keeps animating behind opaque rooms; the manga/eBook hero blur swaps on every focus; a burst of settings-updated events runs 4 engine calls each.
2026-09-25 03:00 UTC: Localization bug pass (subagent): String-typed copy that bypassed lookup now goes through T() or upstream format keys: Home (quick panel, band copy, Continue Watching Up Next/Airing/"Next in", Home row titles), Detail (focused action label, tracker labels, facts and crew labels, video kinds, award counts via the new TCount plural helper), Person, Discover (blurbs, genres, queue deck), phone hand-off, onboarding recap, profile PIN line, play-picker dialogs, library tabs and groups, sports panels, watch and match copy. Engine strings translated with t(): card identity chips and award counts (cards.ts), person sections, Born/Died and department, detail crew labels and "{n} votes" (cache now per language), anime hero pills, playlist series subtitles, sports follow/reminder/base labels; sports webhook reminders format in the UI language (smoke checks). mpv track languages use English names (upstream languageName), not the Apple TV's language; the calendar month heading shown while loading uses L10n.locale. Arabic: back/forward chevrons and arrows use the .backward/.forward symbols, hero side scrims flip (bp-tokens --bp-scrim-side), the player and kids seek bars stay left-to-right like upstream, Left steps the Discovery Queue forward. No catalog keys added (every key used exists upstream in all 15 languages). Coverage (de) 80.1% → 80.9%. Needs device: Arabic symbol flipping and gradient mirroring. Both passes merged: bundle 4488 KB, offline smoke 895.
2026-09-25 03:02 UTC: Settings / Social bug pass (subagent): the edge margin is read through upstream's clampOverscan (0–0.1) in the Settings summary, rows, preview and save (smoke checks; a synced 5 read "500%"); the subtitle language grid uses upstream's 24 common languages plus any picked outside them and allows an empty list (desktop-picked languages had no button and could not be removed; English was forced back); "Test saved key" shows only TMDB's log line with api_key/token/Bearer values masked (it could show another service's request); home-server Remove asks first and is off while syncing, and a desktop "Every N days" interval shows as such (merge kept the localization pass's T() labels). Social: closing the notification center marks everything read like upstream (the bell kept its badge), and a poll in flight can't undo mark-read or an answered friend request; the feed, group and profile pages load once (closing a title or profile re-ran the load: spinner, lost pages, or an offline error page); the groups list keeps only the newest load and later pages skip "Your groups" and repeats; failed group posts show Try again instead of spinning forever; profile comments get Load more; group posts load one page at a time. Offline smoke 897.
2026-09-25 03:21 UTC: Performance pass 2 (subagent): the ambient mosaic now runs only in the one BPAmbientBackground the viewer can see (AmbientCoverage: topmost window, not under a presented cover, a screen's own over RootView's, none under the saver/curfew lock or in the background; covers polled twice a second like PreviewGate); a covered instance takes the mosaic out of the tree and fades it back in when uncovered. Reduce Motion stops a drift already running (upstream useBpDecorMotion follows the setting live). Upstream mounts a single BpAmbient in bp-shell and never uses the "ambient" mosaic variant. Manga and eBook hero backdrops follow focus after the 200 ms BP_META_SETTLE_MS (BPTitleArt.settle) and draw a cover blurred once, off main, from a 240 px decode cached per URL, instead of a live radius-36 blur swapped on every focus. SettingsBridge.load runs one at a time: one in flight plus one pending that later calls join; the boot load starts at once and patch() still applies immediately. Also: 0349759 splits SportsEventView's channel-match line out of the view body (type-check time). Open: Home can run the root mosaic and the band stage mosaic together; the root mosaic runs under the intro wall. Offline smoke 897.
2026-09-25 03:24 UTC: Adversarial regression review of today's passes (subagent, fresh context, b8f1c40..HEAD). Two interaction regressions fixed: (1) a Continue Watching one-press resume left its episode hint on the Detail page, so after an auto-advance Play said "Play S1 E3" over the real resume point and restarted the old episode; the hint is now spent once, like bp-detail's takeBpPlayIntent (c4263b5). (2) Sports "Make it yours" wrote `sportsLeagues` only to the `harbor.settings` mirror, which the new `settings.activate` rewrites at every launch, profile switch and settings-updated event, so the picks reverted; `sports.setLeagues` now saves through persistEffective for the active profile and marks it for profile sync (0c66649; smoke check). Checked, no change: Detail loadStage/autoPlay once-guards, every player save/exit path (4 s tick, flush on pause, background, finish()), SettingsBridge generations and coalesced loads, Live TV generations, the load-once guards, engine return shapes, localization keys. Low-risk notes left open: Live TV now/next can merge a late answer after a quick source switch (cosmetic); three once-flags (AddonPage catalogs, QueueDeck, EBookDetail) are set before success but have nothing to retry. Offline smoke 898.
2026-09-25 03:45 UTC: Mosaic pass (subagent): at most one mosaic runs, as upstream's single bp-shell BpAmbient. `AmbientCoverage` ranks instances root < screen < stage; the new `BPStageMosaic` is used by Home's band backdrop (registers for every band, since it is opaque) and by the new Search backdrop (bp-search: canvas, stage mosaic, idle wash, results wash that sinks it under a query), so RootView's mosaic stands down while either is up. RootView's root mosaic runs only in the adult shell once the intro wall starts leaving (upstream's boot splash, intro, setup and Who's watching are opaque over BpAmbient; a kid shell paints its own canvas), and stays off in search, live and sports (bp-shell's skip list; sports-event is a cover). The PiP browse layer's root mosaic follows its own room the same way. Open: setup's own poster wall (bp-onboard-backdrop) is not ported; Search's mosaic uses the hero feed rather than the Home catalog rows; Home outside a band still shows the root ambient mosaic, which upstream never draws. Offline smoke 898.
2026-09-25 03:56 UTC: Accessibility pass (subagent, 70 files): VoiceOver reads titles on poster/rank/brand/collection tiles and Continue Watching cards (bp-tile aria-label, bpCwCardLabel). Icon-only controls read upstream's aria-labels through T(): player transport (Previous/Next episode, Back/Forward {n}s), kids transport, PIN Delete/Back, keyboard Clear search/Letters/Symbols, profile chip "Switch profile: {name}", Who's watching "Switch to {name}", bell (badge count as value), gallery, X-Ray, steppers, QR codes. The player surface is the Seek slider with a time value and adjustable action. Chips, tabs, options and toggles carry the selected trait through the new `bpSelected` (~80 sites); section titles are headers; RemoteImage art, the hero backdrop, avatars and the brand mark are hidden from VoiceOver. Identifiers and the texts the UI tests query are unchanged; no catalog keys added. Needs a device: VoiceOver on tiles inside Buttons and the Seek adjustable action. Offline smoke 898.
2026-09-25 04:06 UTC: Owner report "the title overlay when hovering a show is behind the cards": the spotlight's copy (title/logo, chips, description, pips) drew under the rail, so rows scrolling up covered it; bp-home stacks its hero (data-bp-home-hero, z-20) over BpRail. SpotlightView gains `layer` (.all/.backdrop/.copy); RoomView draws the art under the rail and the copy and band identity over it (the grid pages and Discover keep .all, their content scrolls under the hero). Also z-order: the focused tile and CW card draw over their neighbours (rows, catalog and addon grids), the row holding focus over the row below (BPRailView heldRow), and the Continue Watching/Live lead over the plain rows. Needs a device check.
2026-09-25 04:18 UTC: Second adversarial review (subagent, fresh context, 0898e0c..HEAD + perf pass 2). One interaction regression fixed (850bd86): with the hero copy now over the rail, Up from a parked row onto Continue Watching, Live or the anime Resume/More Info left that row in the top band under the title and description (use-bp-rail parks every rail row; the port parked only plain rows). BPRailView returns to rest when a lead row takes focus from a parked row (`leadHeld`), and the copy layer takes no hits. Checked, no change: SettingsBridge one-in-flight loads, AmbientCoverage (no leak/flicker), HeroBlur cache, zIndex identity, accessibility grouping/hiding. Open: the root ambient mosaic runs hidden under SpotlightView's opaque backdrop on Home/Movies/Shows/Anime/Discover. TestFlight: build 190 (172467b, hero fix without 850bd86) uploaded 04:17 UTC; build 191 (850bd86) uploaded 04:28 UTC and is the current TestFlight build.
2026-09-25 04:40 UTC: Visual layout pass (subagent, 19 files): Home rail rows parked ~250 pt too high — `scrollTo(key, anchor: UnitPoint(y: (topInset+6)/1080))` lines up the point at that fraction of the ROW's own height with the same fraction of the viewport, not the row's top — so posters sat under the hero title/description and in the top fade (the likely real cause of the owner's "title behind cards"). Rows now park by a 1×1 marker placed topInset+6 above each row (background + alignmentGuide) scrolled to `.top`; route restore scrolls to the row id first (lazy rail), then the marker. Poster titles are drawn inside the art with the bp-tile bottom scrim (the ring hugs the poster; nothing hangs under the hint bar). Fixed-column grids that overran the 1632 pt page now fit: Library 9→5 columns (it ran off both sides), See all/addon pages 6→5, Collections 4×387, collection items 6→5, Sports explore/personalize, profile avatars 14→12, onboarding services 6→4 and taste 6×128. See all and addon scrollers start under the hero (bp-catalog-page) with a ring-headroom mask; Discover draws spotlight art only (bp-discover has no copy over its rail). Focus rings are no longer clipped in Library tabs, the Live band/list/guide channel column, Settings option tracks, the subtitle panel, Calendar and Sports chips. Live row cells take zIndex on focus; Who's watching rings the face only (WhoTileStyle/WhoFaceRing). Needs a device check: rail parking (scrollTo finding the marker id inside a background), See all scrolling (622 pt viewport), Library. Offline smoke 898.
2026-09-25 04:55 UTC: Focus navigation pass (subagent). Home's Live TV "Guide" link vanished as Up gave it the ring (it showed only while a cell was focused); it stays while the row or the link holds focus (bp-row-header). The stream picker disabled every row while a link resolved, throwing the ring onto the filter chips (and leaving it there when the link failed); rows stay focusable and ignore a second pick. The picker opened on the "All" chip; the first row (home-server copy, else the first stream) now takes the ring like bp-streams autofocus / use-bp-focus's 6 s seed pass, also after the Auto step or a dialog. Setup had no Back: Menu closed the app on any step; it now steps back (bp-onboarding pushBpBack), passing over steps the phone delivered (the first step still exits). Search's Recent chips and Clear put the ring on the keyboard (BpRecentRow). A Continue Watching card removed from the quick panel hands the ring to its neighbour, an emptied row to the rail. Backing out of the PIN pad returns to the tile it came from (bp-who-is-watching returnTo). Open: QueueDeck steps the deck on Left/Right even from its action chips (upstream only from the deck cell); Left-at-row-start → tab and Right-at-row-end → See all not ported; ~60 busy buttons still disable themselves under the ring. Needs a device check: picker first-row landing, CW remove focus. Offline smoke 898.
2026-09-25 05:11 UTC: Focus pass 2 (subagent, 31 files): about 59 buttons that disabled themselves while their own action ran (Load more, Add source, Sign in, Save, Refresh, Connect, Install, Test, Sync, Join/Accept, etc.) threw the focus ring off; they now stay focusable, guard re-entry inside the action (checked right before the flag is set on the main actor, so still no double submission) and dim through the new `BPActionStyle(busy:)`. Buttons disabled for other reasons (invalid input, another button's action running, nothing to do, a modal on top) are unchanged. Discovery Queue: a full-width deck cell (bp-queue queue-deck; no lift; Select plays) takes the first focus, and Left/Right step the deck only while it holds focus, so the action chips move normally; the art dims while a chip holds focus; RTL unchanged. Open: finished-state disables that drop focus (EBook Gutenberg added, Calendar tracked person, Collections Saved, Mark all read). Needs a device check: the queue deck cell, busy dimming. Offline smoke 898.
2026-09-25 05:34 UTC: Detail bug pass (subagent). A finished series or anime episode now reads watched: player.saveProgress marks it in the manual store and pushes the series' marks to the Stremio bitfield after its own write, as use-resume-autosave.ts does (episodeWatched.pushToLibrary — upstream's syncSeriesWatchedToStremio needs CompressionStream, missing in JSC; pushes chained, a cloud write waits ≤5 s for a pending push; the strip never showed a ✓ after watching; smoke checks). The strip parks on the resume or next-up card even when it is in the season already on screen, never while the ring is in the strip. Episode cards draw the resume progress bar. Anime Play follows the strip's next-up once any card is watched (episodeWatched.state nextUp; after E1–E5 it said "Play S1 E1"). Watchlist is Harbor's own lib/watchlist (actions.setWatchlist/heroState.watchlist: local, Trakt, Simkl, Stremio) and is offered without a Stremio account. A season past the first eight gets its own chip. The facts card and anime characters are focus tiles, and Facts/Awards dialog lines draw focus (all showed no ring). Rate closes after a score or Remove like bp-rate-dialog; List dialog Create/Cancel keep the ring. A failed reload keeps extras, awards and trackers on screen. The Person page keeps only its newest load. Open: "New list" field focus; character favourite not ported. Needs a device check: strip parking, anime Resume, ✓ after playback, dialog focus. Bundle 4496 KB, offline smoke 906.
2026-09-25 05:50 UTC: Third adversarial review (subagent, 5ae9299..HEAD: layout, focus 1+2, Detail): no regressions. The finished-episode mark fires once per episode with upstream's thresholds (0.85, stub < 150 s, series/anime only), fits the 30 s cloud throttle, and can delay a player close by ≤5 s only right after the 85 % tick. It found an older bug, fixed here: player.saveProgress created missing Stremio library items as removed:false/temp:false, so every title played on the TV became a Stremio Library bookmark and read as "In Watchlist"; now removed+temp like use-stremio-sync.ts (an unwatched temp entry stays removed; existing flags kept; isCwMember still counts them for Continue Watching; smoke check, 907). Open (low): tmdb:-id series never push their bitfield (PlaybackContext.save sends no imdbId).
2026-09-25 06:07 UTC: Player / Live TV device-flow bug pass (subagent, 2a72fd4). The controls' 4.6 s timer restarts when focus moves between buttons (use-bp-player-chrome: every keypress), and never fires under the resume prompt, "Leave the show?", Still watching or an error card, nor pulls focus off the connecting / no-audio / kids-loader cards. Menu only hides controls that are on screen (behind an error card the first Back did nothing). Menu on the Connecting card is its "Go back" (bp-connecting). The source error and connecting cards no longer draw under "Leave the show?" or the resume prompt (a stream failing under the prompt stole its focus), the controls no longer show through "Connecting…", and the skip button / up-next card step aside on error. Every Try again reloads from where playback was (reloadSource resumeAt; it restarted a film that failed an hour in from 0 / the resume point), and a resumed start is kept for later reloads. A stream that ends well short of its length (dropped connection) reloads once from there, then shows the source error card (use-auto-retry premature EOF) instead of a frozen last frame. Live channels opened without a guide (Home row, Search, Sports) get "This channel isn't responding" with Back / Try again. The in-player TV Guide picks its opening group before the first render, so focus lands on the playing channel. Open: live auto-reconnect on error (use-auto-retry live) not ported; a Together guest's transport Next episode isn't gated. Needs a device check: Connecting card / Menu, Wi-Fi drop mid-film, guide focus on big lists. Offline smoke 907.
2026-09-25 06:26 UTC: Music device-flow bug pass (subagent). Rows keep their now-playing mark after a source is found (music-detail.tsx isCurrent matches collectionOrigin; it compared queue keys). A home Up next pick jumps within the live queue (music-personal-bands.tsx queueBand; it replaced the queue with the shelf and dropped the current track, history and radio), and the shelf follows queue edits. Add to queue lines up after the current track and earlier additions and skips duplicates (player.ts enqueueMusic / queue-insert.ts; it appended after the whole collection); Play next on a queued track moves it. Now Playing's queue rows get Play next / Move up / Move down / Remove (music-queue.tsx; engine copy keys + smoke check). Album/artist pages, search and the Spotify library show the dock in a bottom inset (upstream mounts music-dock over every view; a track started there showed no resolving state, no error and no way to Now Playing). The page load error gets Try sources again, search gets Retry. Now Playing opens on Play/Pause and has its own Spotify playlist picker. A music screen's Play/Pause goes to the film in PiP while a video is playing (it started music over the film). A spinner shows while the home first loads. Not ported: a dock on the other rooms, shuffle/repeat modes. Offline smoke 908.
2026-09-25 06:43 UTC: Search / Library device-flow bug pass (subagent). Search: results show only under the query that asked for them (search-display-state; the last query's rows, people and top match stayed up and could be opened, filing the new query as a recent); People and Top match land with the rows instead of a beat later under the ring; opening Search puts the ring on the keyboard once per visit (bp-search-input data-bp-autofocus); AI search state loads once per visit (it reloaded, and could turn AI mode off, on every cover close); an empty field with nothing to suggest shows upstream's "Start typing…" line. Library: the saved sort (settings.librarySort) is honoured (every visit opened on Recent); a new tab starts with search, type and group cleared (bp-library [tab] effect); the Filters and Search chips show an active filter or the query; Menu closes an open Filters/Search/Repair panel first, ring back to its chip; keyboard Done applies the search; Show more hands the ring to the first new tile; empty copy follows bp-library emptyCopy (no Stremio sign-in line on the Harbor watchlist); a failed tabs read on cover close keeps the chip row. Open: query and Library tab not kept across room switches (upstream search-context / bp-view-state); no automatic Library paging; no per-addon Try again. Needs a device check: Search keyboard focus on open, Library Menu/panel and Show more focus. Offline smoke 908.
2026-09-25 07:01 UTC: Kids / onboarding device-flow bug pass (subagent): setup resumes on the screen it was left on (bp-onboarding RESUME_KEY; own key `tv.onboarding.step`, written on every move, cleared on finish; UI tests start on Language) instead of starting over after a power cut, an app kill or a whole-tree rebuild; a Stremio sign-in parked for the first profile is kept in the Keychain (`tv.onboarding.pending-stremio`, not an engine key) and survives a relaunch; the Stremio and Harbor steps show "Signed in as" with Continue once signed in (bp-step-stremio / bp-step-harbor), also when the phone delivered while the screen was up; the Harbor form's late done() no longer skips Layout; the phone hand-off listens to the end of setup (BpHandoffProvider); a new language puts the ring on Continue after the rebuild. Kids: the Play Zone opens with the ring on the first activity and returns it to the one just closed; each activity takes the ring on open; popped bubbles and the win/done cards no longer throw it onto the header's Back (the next OK left the game); the Kids page has Try again when it failed to load; the curfew lock uses curfew-guard's sea gradient and white text (dark on dark under light themes). Checked OK: curfew closes the player and can't be dismissed with Menu, the parent PIN gates leaving Kids, no adult/kid state leak. Upstream parity worth an owner decision: the curfew lock's "Switch profile" opens Who's watching without the parent PIN, so a kid can pick an adult profile that has no PIN. Needs a device check: Play Zone focus, resume after a kill, confirmed steps after a phone delivery. Offline smoke 908.
2026-09-25 07:15 UTC: Settings device-flow bug pass (subagent). The column re-reads synced and panel-made changes: it listens for `harbor:settings-updated` (debounced 250 ms), reloads on slice changes and when a SettingsView cover closes, only the newest load applies (it showed old values, summaries and preview until the next pick). Select pressed before a category's rows arrived no longer opens the previous category's rows. A Display language pick brings the ring back to its cell after the rebuild (it landed on the top bar). The saved column depth is read without side effects and forgotten when Settings is left. Trakt/Simkl keep one button (Connect → Cancel → Disconnect), so a waiting code can be cancelled and the ring stays put. A rejected AniList/MAL code stays in its field. End arrows in Tabs, Home rows and Anime rows, and Show all tabs, dim instead of disabling under the ring. Rename moves the ring to the editor's Save and back. Remove key puts the ring on Connect TMDB and reports a failure. AI search Save and Use model keep their ring. The pane's Version line is translated. Open: the Live TV playlists row still leaves for the Live tab (ST-3); Home/Anime rows panels don't re-read a synced layout while open; the sign in/out buttons still swap under the ring. Needs a device check: ring after a language pick, Trakt Cancel. Offline smoke 908.
2026-09-25 07:28 UTC: Fourth adversarial review (subagent, a124aa4..HEAD: Player/Live, Music, Search/Library, Kids/onboarding, Settings, 85fb08a). One regression fixed (fe4d3e4): a Library sort picked while the first feed was loading was undone when that feed landed; a sort pick now retires the feed in flight. Also (engine) library.setSort now marks `librarySort` for profile sync like bp-library's update(). Checked clean: Settings live reload (no loop/storm; ~3 collapsed loads per commit), early-EOF reload vs natural end / auto-advance / finished mark (one isNatural rule), Menu order with X-Ray, Music Play/Pause vs PiP film, onboarding resume vs Fixtures and finished installs, handoff teardown, Kids identifiers, no Menu swallowed at a root, Stremio removed+temp matches use-stremio-sync. Open (low): Trakt Cancel turns into Disconnect under the ring on approval; the pending Stremio sign-in in the Keychain can outlive a reinstall. Offline smoke 908.
2026-09-25 07:34 UTC: Sports device-flow bug pass (subagent). The consent notice no longer flashes for consented viewers (consent starts unread, not "unknown"), and it is laid out like bp-sports-consent: title pinned, notice scrolling with "Read the rest", acknowledgement and actions pinned (the first focus used to scroll the title away for good). The schedule date band follows date-bar buildDays: 7 days back, 7 ahead, weekday over the day of the month (smoke checks). The hero cycle holds while the hero has focus and with Reduce Motion (use-bp-sports-cycle). The event page offers "Set up Live TV" (setup plan, picker, broadcasts list; opens Live TV) and "Try again" when details fail (the engine no longer caches a failed summary for 25 s; smoke check). Standings are upstream's single windowed cell for the teams' group with Show all / Show less (it had 20 ring-less focus stops and a group-chip header off the page), and picking a channel closes the picker. The who page's bio is no longer an invisible first focus. Make it yours: Menu steps back a step. The feed note goes through t() in upstream's "… · names" shape; the status time uses the UI language. Needs a device check: consent paging, hero focus hold, the Set up Live TV hand-off. Offline smoke 912.
2026-09-25 07:50 UTC: Watch Together device-flow pass (subagent). A host moving to another episode or source (Next/Previous, auto-advance, Still watching → Continue, Sources, Switch source, Pick another source, stub eject) no longer tells the room it left the video with cleared media: the relay had handed the host role to a guest, so the next episode opened under a foreign host with no invite (upstream opens the picker over the player; closePlayer never runs). Follow-up (mine): that reopening close publishes the spot paused, so guests don't play on unseen while the host picks, or after the host backs out. TogetherPlayback follows the player's current engine (room sync died after Try again, the dropped-connection reload or the move to mpv). The room's speed syncs (host publishes its rate; guests apply state.speed 0.25–4×, not remembered). In the background without PiP the room no longer restarts a paused guest or drives the host; it resyncs on return. A room guest gets no resume prompt/seek (use-bridge-load guestInRoom). A guest's Previous/Next episode and the remote's track commands follow canChangeEpisode (use-queue-nav). A stale "left the video" notice clears when a player opens or closes; each "left the room" toast gets its own 4 s. Open: a TV guest must press Back twice to follow the host's invite to the next episode (its countdown toast is hidden under the title page); host-source matching not ported. Needs a device check: host advance/Next with guests, guest Try again, desktop host at 1.5×, TV button mid-room. Offline smoke 912.
2026-09-25 08:06 UTC: Manga / eBook device-flow pass (subagent, e55f015). The manga reader's page count moves into the bar header while the bar is up (it sat under the bar's bottom panel) and fades after a chapter lands; zoom buttons dim at 0.5×/3× instead of disabling under the remote; Retry returns focus to the page; the reader saves and stops prefetching when it goes away without Close. Manga detail: one Resume / Start from beginning button, so focus survives the reader closing; Show more moves focus to the first chapter it reveals. Manga sources: "Use this server" becomes an Active button, and Add server isn't disabled by the form clearing after an add (upstream server-form). eBook reader: the bar is two rows (nine labelled buttons overflowed the screen); the count sits in the bar header; a failed chapter's card takes focus with Try again / Next chapter / Close reader (the full-screen surface covered it); Stop and bookmark delete pass focus on; settings steppers dim at their bounds; the side panel clears the title-safe area. eBook detail: Start Reading stays focusable while chapters load (an early Select shelved the book). A book picked on the Shelf opens after the Shelf cover closes (the second cover was dropped). Gutenberg quick add stays focusable once added. Open (low): Remove on a manga server / eBook source row and the manga browse Try again still let focus fall to a neighbour. Needs a device check: two-row eBook bar, header counts, failed-chapter card, Shelf → book. Offline smoke 912.
2026-09-25 08:09 UTC: Discover / Voyages / Calendar device-flow pass (subagent). Discovery Queue: the Discover band and the deck share one order per pool build (use-bp-queue orderedPool; the band shuffled its own, so its fan and backdrop named other titles, and the deck, cached per key, never took the next day's pool; smoke checks), and the band re-reads it when the deck closes (`discoverRoom.queuePeekFor`). Save toggles Harbor's watchlist (actions.setWatchlist; worked only with a Stremio account, could not be undone, never showed Saved) and reads Saved per card; Not interested asks "Hide this permanently?" first (BpConfirm); focus is seeded on the first open only and goes to Back to Discover when the last pick is gone; pool tags translated. Discover: Try again when the build fails, rail titles through t(), "All movies"/"All shows" header chips leading to the tab (BpRowLead; BPRowView gets `seeAllLabel`), genre art fetched in one pass, Genres blurb key. Anime award: late answers from a previous source are dropped. Person: the Sort/Rating rows stay while the person has credits (a rating no title cleared removed them and the focus), upstream's empty states, loads once, Try again on failure. Calendar: a source pick keeps the chip rows and shows the skeleton only in the grid (the pressed chip vanished under the ring), Try again on the error card, the day view's long date in Harbor's UI language (smoke check). Voyages checked: no defects. Open: Calendar filters Clear all / tracked-person chip still drop focus. Needs a device check: queue confirm + Save, Discover rail header chip, calendar source switch focus. Offline smoke 917.
2026-09-25 08:23 UTC: Addons device-flow pass (subagent): a fresh catalog read no longer reuses the build already in flight and a late older build no longer replaces a newer one (the Installed tab missed a just-installed addon or kept a removed one; smoke check). The Addons screen re-reads the kept catalog on every visit like addons.tsx (`cached` flag; addons added/removed on another Stremio app appear without a relaunch; smoke check); `loadCatalog` is numbered. Home drops its kept catalog rows on harbor:addons-changed and reloads on it like home.tsx (smoke check). Addon detail: one action button changes in place and stays busy until the page re-reads the addon (focus jumped to Reveal); Back clears the page it left; a re-read keeps the open sections and no longer closes the page after a QR cover; the toaster shows over the detail cover. Install dialog: Install/Update stays (busy) while installing and takes focus when a link resolves; Done takes focus on success. Installed rows keep their buttons while Remove runs ("Uninstalling"). Organize focuses a row moved to either end, and the first row after a restore. A stream-only addon page has bp-addon's Back. Open: browse-list errors still read "No addons match"; a stremio:// link arriving while another cover is up can't present. Needs a device check: detail/install focus, Organize ends. Offline smoke 920.
2026-09-25 13:41 UTC: Sports addon panel streams per listing (subagent, 43f6f6a). Pick A, B, then A again (all slow): B's answer landed first and the one global pick list dropped A's first answer, so the A streams on screen said "Could not start" until A's second answer landed (smoke check). The engine now keeps each listing's streams (the last 8 listings, each listing's newest landed pick), and Play looks them up by the listing key the panel already sends; an earlier pick landing after a later one returns the later streams, and the panel drops an answer numbered below the one on screen (shownPick). The pass-2 slow-listing and review-12 same-listing-twice checks still pass. Device check: pick a slow listing, Back, another, Back, the first again, then Play as soon as streams show.
2026-09-25 13:41 UTC: Onboarding "Leave setup?" + Search pass 3 (subagent, a90ba7c). Setup: Menu on the Language step opens upstream's BpOnboardLeave (Keep setting up / Finish later / Do not show this again) instead of closing the app with no prompt; Menu with it up closes it and the ring returns where it was; Finish later leaves setup for this run only (AppModel.suspendOnboarding: first profile, parked Stremio sign-in attached, the saved step kept so the next launch resumes there), Do not show this again finishes setup. The Harbor step opens with the ring on Later (bp-onboard-steps focus "skip"). Search: the query line is a real text field under the drawn line (bp-search-input), so the tvOS keyboard and Siri Remote dictation type into it and search (there was no system field, so no voice input at all); Top match is search's topMatch alone and opens on Select (it showed the last focused tile and could not be opened); People follows Series unless the query names the first person (matchPersonForQuery); a late addon row keeps the announced installed order; offline says "TMDB is temporarily unavailable…" (smoke checks) and names addons that did not answer; hold Select on a recent query removes it. Open: a Try again for failed addons; placeholder rows for addons still answering. Device check: dictation into the search field, Top match select, the Leave dialog and where the ring lands, Later on the Harbor step, focus back on a person tile after PersonView. Offline smoke 1007.
2026-09-25 13:32 UTC: Review 13 of the music shuffle/repeat batch (6b01a08) and review 12 (8314373) (subagent, 531cb93). Plain playback took the Play next pick again at the advance: a pick of a track already heard jumped back to its earlier copy and the album replayed from there, Previous then skipped the entry between, and Move down of the pick was ignored. The pick now counts under shuffle only, and the copy after the current entry is taken (MusicQueueOrder.pickIndex). Under shuffle the pick joins the dealt order right after the current track (the tracks dealt between were skipped, and Repeat off stopped early). Play next on a row before the current entry (Repeat all's wrap) moves it after the current one, as upstream's playNext(index) does. Checked clean: plain next/previous/queue end/radio/Spotify/gapless hand-off, Repeat one with the preload, auto-skip under Repeat all, remote commands registered once, persisted modes (upstream readTransport), kids dock inset and focus, Now Playing close label, roomLayer clearing (profile switch, rebuild, covers, PiP layer's own AppModel), SocialRow seat call sites, sports addonPicked (both smoke cases). Open: pick A, B, A again (all slow) briefly holds B's streams (as before); Repeat one hides the queue on Now Playing (upstream parity); a shuffled radio append can reshuffle past a queued gapless item. Device check: Play next on a heard track, Play next under shuffle then Repeat off to the end, Play next on a wrapped row. Offline smoke 1002.
2026-09-25 13:20 UTC: Music shuffle / repeat (subagent, 6b01a08). Upstream's music-queue.tsx modes and queue-order.ts are ported (MusicQueueOrder.swift): shuffle deals the listening order up front with the playing track at its head and keeps the stored order (off plays on in album order); Previous under shuffle walks back through what was heard; Repeat cycles off → all → one (one replays a track that ends by itself, Next still moves on, all wraps and deals a new shuffled lap); a Play next pick plays next in any mode; automatic skips of unplayable tracks follow the order and can't loop under Repeat all; the gapless preload asks the order for the next track and is dropped when a mode changes. Modes persist (harbor.music.shuffle.v1 / repeat.v1, durable) and drive tvOS's change shuffle / repeat mode commands (TV-only). Dock and Now Playing get Shuffle and Repeat (upstream labels via engine copy keys + smoke check; Now Playing's close is now an icon); the album and Spotify pages' Shuffle toggles the mode instead of playing a shuffled copy, and Play is Play/Pause for the queue playing (music-collection-controls.tsx). Now Playing's Up next lists what will really play (musicUpcoming), with the shuffle note; Move up/down only without shuffle. The kid shell shows the music dock (App.tsx mounts it over every view), closing the "no dock under a kid" open item. Device check: gapless hand-off into a shuffled / wrapped next track, Repeat one on stream and Spotify, the Siri Remote / Control Center shuffle and repeat controls, dock focus on the kids page. Offline smoke 1002.
2026-09-25 13:15 UTC: Review 12 of Sports/Addons pass 2 and the Social/Collections/Reminders pass (subagent, 8314373). Engine: a listing picked twice (Back, then the same listing) plays from its first answer while the second loads; the newest-pick rule dropped that answer and every Play said "Could not start" (smoke check). The addon catalog page's shown chip retries over an empty grid (a failed first page had no retry). A reminder remove whose answer can't be read re-reads the list instead of dropping the row and saying "Reminder removed". Collections room: while the overlay is up the top bar takes no focus, LB/RB turn no tab and the hint bar drops Tabs (AppModel.roomLayer). Save to my collections at the 24-collection limit says "Limit reached · n / 24". Notifications: after Accept/Decline the ring goes to the next request's profile tile (not its Accept), and after Dismiss to the row now in place. A double press on a comment or group-post like sends one request. Checked clean: Sports mode per profile, midnight rollover once, hero hold, addon panel seeds, setup prefill once and deep link, AddonDetail's late re-read guard, setup cover gating, the room's disabled grid (the overlay is outside it), focus restore after delete, optimistic Dismiss converging, group pages without repeats. Also: a safety review of the lazy storage namespaces (5f97a54) found no data-loss, boot or sync risk. Open: TV collection edits are not published (owner decision). Device check: overlay focus lock, notification ring hand-off, the limit note. Offline smoke 1001.
2026-09-25 12:59 UTC: Review 11 open items (subagent, 5f97a54). The media-server details store is bounded: after each Library read, titles no stored server holds are dropped in every language, then the least recently read past 3,000 (the read's own never), tracked in an index key; the details and the index are lazy namespaces (shims/storage.js LAZY_PREFIXES = KeyValueStore.lazyPrefixes), left out of the boot snapshot, read with storageGet on first use and not memoized in Swift (storageClear lists keys with engineKeys()). A Media Servers filter pick during the restore read shows at once and is applied on top of the restored filters when the read lands, instead of saving the defaults over the saved server/library/genres. hydrate-meta lookups run only while the settings mirror localizes like the profile settings the cache key is made from (upstream's key source), so one language is never stored under another's key. Device check: launch time with a big library, Media Servers after a relaunch (details present, no refetch), a filter pick right after opening Media Servers, language change then Library. Offline smoke 1000.
2026-09-25 12:57 UTC: Social / Collections / Reminders device-flow pass (subagent, 1124337). Collections room: the in-place overlay no longer leaves the ring on the card underneath (the grid is disabled while it is up; Select re-opened the card); the overlay seeds focus like bp-collection-items / bp-collection-detail (Close, or the first TMDB tile), starts below the top bar (the bar covered its name), and hands the ring back to the card on close. Delete's prompt lands on Cancel (a double press could delete); panels, Rename and Remove titles keep the ring. Save to my collections dims instead of disabling, runs once, and is gone on the member's own shared collection (community-hub isOwn; engine own + saveCommunity refuses; smoke checks). Create runs once in the room and in Add to list; See every TVDB list hands the ring to the TVDB chip; new collections are named with t("Untitled collection"). Notifications: Dismiss / Clear all are optimistic like use-notification-center (offline the row never went); a poll started during Mark all read no longer brings the badge back; Mark all read / Clear all dim instead of disabling; offline the center offers Try again instead of Loading forever; the account menu reseeds after its refresh. The profile friend button and group Join / Accept keep the ring on the replacement button; accepting from a profile updates the bell. Closing a group keeps the pages Load more added (and the list on a failed re-read). A failed reminder remove no longer empties the manager. Open: TV collection edits are not published (upstream publishCollections on back; owner decision — collections aren't profile-synced and an ungated publish can overwrite the account's collections); overlay Up/LB-RB still reach the bar; the ring after a notification row goes. Device check: overlay focus seed/restore, Delete → Cancel, friend/group focus, offline Dismiss. Offline smoke 997.
2026-09-25 12:46 UTC: Sports / Addons device-flow pass 2 (subagent, 2b7f97a). Sports: the mode is kept when the viewer leaves the tab, per profile (bp-view-state sportsMode; it came back on For you). The date band and a picked today follow midnight (use-bp-sports today timer). The hero keeps the game on screen across score refreshes and on return from an event (it jumped to the first game under the focus). Hero, event and card scores follow upstream's scored rules (no "0 : 0" for postponed games, no frozen score on saved cards). Decline opens Live TV like bp-sports-consent. The addon panel's focus goes to the first listing, the first stream when streams load, and the first listing after Back (it opened on the search field). Engine: a slower earlier listing pick no longer replaces the newest pick's streams, which made every Play fail (smoke check). Addons: the setup dialog keeps a link sent from the phone (the prefill ran again when the phone sheet closed and wiped it) and its success card closes itself again. The addon page opens on its action button (it opened on the star count's QR), and a late Remove re-read can no longer close the next addon's page. An Install that answers with a setup page opens it only when nothing else is up. The addon catalog page ignores a press on the chip already shown (mid-scroll it restarted paging out of order). Open: consent autofocus (it would steal the top-bar ring), Browse errors, a stremio:// link over another cover. Device check: addon panel and addon detail focus, the phone link in setup, midnight rollover. Offline smoke 992+.
2026-09-25 12:44 UTC: Eleventh adversarial review (subagent, b40d1a5, 2c965bf, 5f6d7bd, 0438f63: VoiceOver pass 2, tracker notes, Library owned sorts, Media Servers details). Six regressions fixed (08b4760): a media-server lookup queued before a language change fetched the new language's details and stored them under the old language's key for good; it is now skipped, and so are lookups the latest Library read no longer wants (another profile's locale, a removed server), so they no longer fetch ahead of the titles on screen; a details refresh no longer starts a whole-feed read while one runs, and the gap grows with the library (3 ms a title, 1.5–15 s); the re-sort hand-off pulled the ring back from Show more (it now runs only when the ring's tile left the page); Library tiles read their resume bar and marks together (the button's empty value hid "Watched"/"In watchlist"); the saved Media Servers filters are durable (a Caches purge reset them). Checked clean: every hidden/combined container (no focusable children), identifiers and UI-test texts, feed request/response keys, owned sort only on Media Servers, Show more across re-reads, genre restore offline, no-key lookups, every noteOk site. Open: the per-title metadata store is never pruned (RAM copy plus boot snapshot); a filter pick during the restore read overwrites the saved filters; hydrate-meta localizes from the mirror while the cache key uses the profile. Device check: language change during lookups, Show more while details land, VoiceOver on a watched Library tile. Offline smoke 994.
2026-09-25 12:20 UTC: Library Media Servers details and Library/Genre rows (subagent, 0438f63). Media-server titles are looked up the way bp-library does (use-bp-library useMediaServerEntries → hydrate-meta: TMDB details for a tmdb: id, else the Cinemeta/addon lookup), stored per title and locale, so Rating, Duration, genres, posters and years have values. Unlike upstream's all-at-once wait, titles show at once and the lookups run in the background 6 at a time (tmdb-client's scheduler limit); a miss waits 10 minutes, titles the server could not match are never looked up, and harbor:media-server-details (at most every 1.5 s) re-reads the feed so the owned sort re-applies (engine homeServers.libraryFeed; smoke checks with fixtures). The Library ("All libraries") and Genre rows are ported with upstream's labels, a count on every option, a title needing every picked genre, and library/genres saved and restored through filter-preferences. Filter chips have focus ids: a chip or row that goes away under the ring hands it to that row's active chip or the next row, 0-count chips are dimmed but stay focusable, and a re-sort that pushes the focused tile off the page hands the ring to the tile in its place. Device check: Media Servers Rating/Duration after details land, Library and Genre rows and their restore, ring hand-off. Offline smoke 991.
2026-09-25 12:04 UTC: Library owned sorts and note flags (subagent, 5f6d7bd). Media Servers sorts like bp-library's owned tab: Date added / Title / Year / Rating / Duration plus a Direction row (Ascending/Descending), default Date added descending, one list with missing values last either way (engine sortOwned; smoke); its type, server, sort and direction are saved per profile through upstream's filter-preferences key and restored when the tab opens, and a title counts under every server that has it. The Library group row comes first and is headed by the tab's own label ("My Lists", "Media Servers", "Trakt"…) instead of "List"/"Group". The Anime4K, home-server and API-Sports key notes are coloured by an ok flag set with the note (Anime4K's translated "Download failed" had read green; "Some shaders are still missing." is now red); the player's chip(active:) carries the selected trait. Open: media-server titles aren't looked up on TMDB as upstream does, so Rating and Duration find no values and keep the server order; the Library and Genre rows are not ported. Device check: Media Servers Sort/Direction and restore, group heading, Anime4K download note. Offline smoke 974.
2026-09-25 12:00 UTC: Tenth adversarial review (subagent, 306bf2d..HEAD plus 57bf553: player clock pass, Detail room invite, localization gaps). Two regressions fixed (6ac33ed): the in-player TV Guide's "{n}m left" and airing bars froze while it was open (they read the wall clock and moved only because the whole player redrew each second; the panel now redraws every minute); the room screen's toast host did not count the summon title page it presents itself, so an invite auto-joined unseen under it (the present was dropped and the invite spent), and an invite to that page's title also got the page's own toast. Checked clean: every PlayerScreen body read of the clock, Play/Pause, X-Ray and the hide timer on isPaused, the connecting card's beat, the sleep chip and speed panel, Now Playing and remote commands, status stored on change only (no whole-struct watchers), error cards, PiP placard, kid loader, still-watching countdown, Multiview's own status handler; no double invite between a Detail page and the shell, the PiP layer and room screen hosts after the TogetherInviteCard extraction, no focus taken by the toast; T() format keys match their arguments, "Cached" still compared untranslated. Open: a tuned channel's chrome subtitle refreshes on the next redraw; a Detail toast whose 4 s ends during the page's dismissal. Device check: TV Guide "m left" with the panel open, summon Sure then invite in the room screen. Offline smoke 966.
2026-09-25 11:54 UTC: Tracker notes and VoiceOver pass 2 (subagent, 2c965bf, b40d1a5). Trakt/Simkl, AniList/MAL and Settings' TMDB test notes are coloured by a noteOk flag set with the note (LetterboxdPanel's pattern), not by hasPrefix("Connected"/"OK"), so the success line is translated: "Connected as @%@" (trakt-device-modal), "Connected as %@" (simkl/anilist/mal), "Connected to Trakt/Simkl" when there is no username. VoiceOver: drawn resume bars read upstream's "{n}% watched" (new bpProgressValue: Detail Play, episode strip, Library history, playlist VOD episodes, Voyage slots); drawn picks carry the selected trait (player track/speed/sleep/quality rows and chips, calendar chips, Trakt sources, collection picker, rating 1–10, add-to-list, AI model/provider, theme/font tiles, anime chips, sports teams/leagues/reminders/follows, onboarding languages, tab locks, age gate, reader bars, Sub/Dub, AI search); upstream aria-labels through T() on the guide star, guide blocks, a collection tile while editing, manga "Saved on your server", tiles' "Watched"/"In watchlist" value, the lightbox surface, the music bar and "Setup progress"; state glyphs named and about 120 decorative symbols next to text hidden; focusable text blocks read as one (76 files). Identifiers and the texts the UI tests query are unchanged. Open: PlayerScreen's chip(active:) state; the Anime4K / home-server / sports-key notes still pick their colour from their English wording. Device check: VoiceOver on the guide, the player's track dialogs, a tile with marks, the collection editor. Offline smoke 966.
2026-09-25 11:32 UTC: Localization gaps pass (subagent, 1cabd7e). A String shown with Text/Button is never looked up, so copy that reached the screen as a String variable or behind ?? stayed English in every language even where upstream's catalog translates it. Now routed through T(): the controller-connected toast, Home's collection card "Collection", the picker's "This source" (P2P dialog), "All addons" chip, facet chip labels (bp-stream-chips t(f.label)) and "Cached" badge, Live TV's "Sources", Kids "Just for kids", Play Zone activity names/blurbs (play-zone.tsx t()) and kids-detail genre chips, the PIN pad title ("Parent PIN", "Enter %@'s PIN"), Calendar "Failed to load", Manga "Untitled", the eBook "Chapter %lld" fallback, the shared-list subtitle and a group post's "Pinned" (13 files, 18 lines). The rest is either already T()'d at its sink or has no upstream key. Open: the tracker "Connected as" notes stay English because their colour is chosen by hasPrefix("Connected") (needs a noteOk flag like LetterboxdPanel). Offline smoke 966.
2026-09-25 11:31 UTC: Detail room invite (subagent, 9a9daa1; review 9 open item). A guest reading the invited title's Detail page, or getting the relay's repeat invite after a join or reconnect, no longer gets the picker at once with no Dismiss. The shell's toast waits under any cover and a Detail page is one, so the page now shows upstream's toast itself (TogetherInviteCard, shared with TogetherToastHost: "{name} started watching" or "Pick your source", Join or Choose, Dismiss, 4 s auto-join). It joins at once only within 5 s of its own player closing (the host moved on to the next episode, no second Back). A re-invite to the video that just closed, landing while the player was closing, is dropped. The invite waits under the page's covers, the saver or lock, a context menu, alert or focus outside the page (DetailPageProbe), and the page's own room autoplay. Natural-end auto-advance keeps review 9's open-picker guard and leaves the next episode to a pending room invite. Also: the player's Ends readout ticks each second while paused (89b9ef6). Open: an invite to another title still waits under a Detail page for the shell toast. Device check: hold-Select menu during an invite, next-episode follow, reading the invited title's page, reconnect re-invite. Offline smoke 966.
2026-09-25 11:20 UTC: Ninth adversarial review (subagent, 6294773..81fe5ff: perf/memory pass 3, Watch Together pass 2, Discover/onboarding pass 2). Two regressions fixed (95f1be7): a TV guest said "ready" again after a host moved on to the next episode (the fresh claim marks everyone not ready, but the room state still named the old episode until the host's new player published), so the host's lobby read the guest as loaded while it was still on the old episode; the re-send now waits while an invite to other media is pending and is capped at 3 until the room lists the TV ready. The natural-end auto-advance no longer replaces a picker the Detail page's room-invite follow already opened (guest-pick rooms autoplayed). Checked clean: ImageLoader shared downloads with one waiter cancelled, re-appear after a cancel (no negative cache), memory-warning purges, Now Playing artwork, the hero-cycle gate (re-read every tick), the stall check vs seeks, pauses, live and end of film, same-video invite drop vs other episodes, catch-up buffer maths, toast hosts, the Detail follow guards, the language re-seed, the recovery-code return, calendar skeleton, Discover park ids, Voyages, manga derived lists. Open: a guest reading the invited title's Detail page gets the picker at once with no Dismiss (upstream: a 4 s toast); People arriving late still pushes a focused rail row; a thin-buffer guest source stays in catch-up (upstream). Offline smoke 966.
2026-09-25 11:17 UTC: Performance pass 4, player clock (subagent, 57bf553). PlayerScreen no longer redraws its whole body every second during playback. Three things did it: the 1 s tick's snap and buffered went into root @State; both engines' 1 s status report was stored whole, though its fps/dropped readout (position, cache) is never shown; and the root observed the SleepTimer countdown. The tick's snapshot, buffered end and lastGoodPos now live in PlayerClock (Player/PlayerClock.swift), a plain @State object the body does not observe; the seek bar, readout ("Ends" follows the wall clock each minute), kid transport, resume fork, skip pill / up-next card, Home server quality panel and the stage's VoiceOver value observe it as leaves, and the logic (saves, skip detection, up-next, Switch source, finish, scrobbles, Now Playing, Watch Together) reads it directly at the same moments. The root keeps a change-only isPaused for the Play/Pause chip, X-Ray and the hide timer; a status report is stored only when state, picture, error or log changes; while loading, a tick beat keeps the connecting card's 2 s / 8 s / 22 s steps; the Speed & sleep chip observes the sleep timer itself. Device check: seek bar/readout playing and paused, skip pill and up-next countdown with their transitions, connecting card seconds, kid time bar, resume fork duration, sleep countdown chip, chrome hiding after a remote/host Play, VoiceOver Seek value. Offline smoke 966.
2026-09-25 11:04 UTC: Discover / Voyages / Calendar / onboarding device-flow pass 2 (subagent, a3ed212). Setup's Language ring opens on the current language again at launch: the intro wall disables the tree under it (bp-shell only opens setup once the intro is done), so the seed was dropped and tvOS put the ring on English, where one OK switched the language; the step re-seeds when the tree becomes enabled. A late TMDB key check or Stremio sign-in advances only from its own screen (Use Cinemeta instead or Not now pressed mid-call, or Back, jumped the viewer past a step they never saw; the key and the sign-in are kept). A new Harbor account's one-time recovery code that lands after Later or Back brings setup back to the Harbor step, which reveals it. Discover: Top People follows Genres and Voyages as in bp-discover's entry order (the last lead to load, it dropped in above Awards and Genres and pushed the band holding the ring down). Voyages: a theme that would not chart keeps the ring on its tile. Calendar: Previous/Next/Today show the skeleton until the new month answers (the last month's grid stood under the new label, and a day pressed opened last month's releases). Open: upstream's "Leave setup?" on the first screen; the Harbor step's ring on Later; a double Continue into a still-loading step; Discover Try again focus. Device check: Language ring after the wall, Create account then Later, calendar month skeleton, Discover band order. Offline smoke 966.
2026-09-25 11:02 UTC: Watch Together device-flow pass 2 (subagent, a8ac757). A guest still buffering after a sync seek is no longer seeked again at every heartbeat: the catch-up read bufferedSec() (the buffer's end) as seconds ahead, so "under 2 s" never held and "near the end" held past the film's half; it now uses the seconds held ahead of the playhead, as upstream does. A host whose stream stalls sends no heartbeat (use-room-sync publishes only while playing or paused), so guests are no longer sent back once a second. A host's Play publishes at once, so guests no longer start from a stale lobby seed. The player drops an invite to the video it already shows (the relay re-invites on every join and reconnect), so the film no longer reopens after the guest leaves the player. A TV whose picture is up says ready again after a host's fresh claim (source switch, reopen). "Leave the show?" no longer pauses a room guest, and Keep watching leaves the guest's video to the room. The room screen's toasts wait under its own title page or typing sheet, and the PiP browse layer's toasts check the layer's own window. A Detail page on screen follows a room invite to its own title (the host's next episode) without the second Back. Open: stale queued state after a reconnect and a host back from sleep reclaiming the room (both upstream behaviour), the host's resume fork vs the lobby seed, no kids Detail follow, host-source matching. Device check: host buffering, slow guest source, guest leave dialog, host source switch, network drop, next-episode follow, invites during PiP. Offline smoke 966.
2026-09-25 10:48 UTC: Performance/memory pass 3 (subagent, 37aa626). The app now reacts to tvOS memory warnings: ImageLoader drops its decoded images and the URLCache RAM copy, the manga reader its decoded pages (up to 160 MB), the blurred heroes and kids art go too, and the engine runs a JavaScriptCore collection (HarborEngine.collectGarbage); disk caches and images on screen stay (nothing listened before). ImageLoader cancels a download and decode once nobody waits for it (a poster scrolled off or its URL changed), so a fast scroll through a long grid no longer queues every poster it passed ahead of the ones on screen. Home's hero cycle pauses while Home is covered (player, Detail, account menu), under the saver/curfew lock and in the background, but not while PiP browsing (it decoded a new backdrop behind the video every 7 s). The music album/playlist track list is lazy. Manga detail builds its filtered and sorted chapter lists once per change (about 10× per render before, on every focus move). SocialCenter's 60 s poll publishes only on change. Noted for a player pass: PlayerScreen's 1 s snap @State and the SleepTimer countdown re-render the whole player body every second. Device check: posters after a fast scroll, hero resumes after Detail/player, simulated memory warning. Offline smoke 966.
2026-09-25 10:42 UTC: Eighth adversarial review (subagent, 17b79d9..6bb4f03 plus 3eddbee: Player pass 2, Kids/Music/Manga pass 2, review 7 open items, stream-bundled subtitles, device-flow pass 3). Two regressions fixed (6622588): Play/Pause under "Leave the show?" forced the video to play, so a Watch Together guest under a paused room started alone (it went through interceptToggle before); in a room it now closes the dialog like Back. Letterboxd Connect pulled the ring back to Disconnect after a slow Stremboxd check even when the viewer had walked to another Settings section; the ring follows only if it is still on the step. Checked clean: the capped close wait (engine local writes, watched mark and CW land before the first wait; scrobble stop, home-server report/stop and the Stremio push carry on after onClose; no close hangs), Play/Pause under the resume fork, Now Playing and PiP, the Menu→stage ring, the panel ring restore, dialog track polling (no refocus, stable rows), seeds once per controller (retry, move to mpv, switch, Together reload), a saved or session Off and viewer picks never overridden by a seed, no seed carry-over to the next episode, addon subtitles whitelisted (no trustedSource), sameOrigin by scheme/host/port, failing seed downloads skipped, Kids Play seed and failed-page keep, Library View row / Year sort vs bp-library, per-search finished, holdPartials smoke-only. Open: a trusted seed's token kept across a redirect; the secondary-subtitle auto pick not re-run for seeds; the plan-in-flight pick window for the seed plan. Device check: guest Play/Pause under the leave dialog, Letterboxd Connect then walking away. Offline smoke 966.
2026-09-25 10:26 UTC: Device-flow pass 3 (subagent, 9d9b369). Harbor and Stremio Sign out and the Letterboxd Disconnect ask nothing first, like upstream (account-identity-card, stremio-card, letterboxd-panel handleDisconnect: no dialogs); the ring now follows to the section's Sign in (stremio-card returnRing) or to Letterboxd's Connect/Disconnect, and Letterboxd Connect dims instead of disabling on an empty field. Library filter labels follow bp-library: Shows, A-Z, Year only when a visible title has a year, View only for Recent with a dated title outside Media Servers, and the History row is "Episodes" (the engine feed gains dated/years; smoke checks). Person: the empty state's "Any rating" hands the ring to the Rating row's chip. Picker: the sort chip dims and ignores presses instead of disabling when an addon ranks its own list, with bp-stream-chips labels (Addon order (locked) / Addon order / Harbor pick). Settings' sync queue line, "For the … profile" and "Last pulled …" stay English: upstream has no catalog keys for them. Open: the Library group heading (upstream uses the tab label) and the Media Servers owned sorts/Direction row are not ported. Device check: ring after Sign out and Letterboxd Disconnect, Person "Any rating", Library View row when switching sort or tab. Offline smoke 966.
2026-09-25 10:24 UTC: Stream-bundled subtitles (subagent, 7202a85). A resolved stream's own subtitles (view.ts PlayerSrc.subtitles: an addon stream's subtitles, a home server's external files, now trustedSource, a P2P stream's addon subtitles) travel from Detail, Kids Detail, the Sports addon panel (direct and handoff) and the in-place kid / home-server quality switches into PlayerScreen (streamSubtitles; a switch brings the new stream's own, a tuned channel none). Every player adds them unselected after its first track plan, as mpv.ts addSeedSubtitles does: upstream's check (trustedSource, else isSafeProviderSubtitleUrl: public http(s), no credentials), prepareSubtitle through the engine (subtitles.prepareSeed, cached by URL; a trusted seed on the stream's own origin gets the server's token), then sub-add … auto on mpv or the overlay's sideloaded list on AVPlayer. Once the batch is in, the track plan runs again with them autoSelectionEligible: the preferred language can pick a seed only when no track of the file's own ranks above it, the episode's remembered seed is selected instead of restored twice, and nothing changes after a viewer's pick or over an automatic subtitle unless subtitleAutoUpgrade is on. Open: the secondary-subtitle auto pick is not re-run for seeds. Device check: a Stremio addon stream with subtitles (dialog rows, auto-pick) on both engines, a Jellyfin/Plex external .srt. Offline smoke 964.
2026-09-25 10:21 UTC: Review 7 open items (subagent, 7e017d0). An anime partial built through the async enhanceAnimeStreams that lands after the search's final result no longer replaces the engine's lastResults and is no longer sent as a partial: a search is marked finished once it answers (final, error or abort), matching Swift's finalGen (a smoke-only streamsRoom.holdPartials lets a held partial land after the final; the check fails without the fix). Simkl disconnect also turns useSimklAvatar off, as simkl-panel.tsx does (smoke). In Home rows and Anime rows, a sync that removes the row being renamed closes the editor; a ring on the editor's buttons goes to the Rename of the row now in its place (the row above at the end; for Anime, Tune's Hide-watched switch when no row is left), and a Save or reset that returns after that no longer aims the ring at the missing Rename. Device check: rename a Home or Anime row while another device removes it. Offline smoke 952.
2026-09-25 10:15 UTC: Kids / Music / Manga device-flow pass 2 (subagent, 73a636e). The kids detail page seeds Play before its load: the episodes landing pulled the ring back up to Play from More to explore or the season chips. A failed kids build (offline at launch) no longer wipes last session's shelves; it builds again on the next visit. Manga reader: Reading mode leaves a detected webtoon strip (manga-reader.tsx setMode clears autoLong; the label stayed "Long strip" and nothing changed); the failed-chapter card offers Next chapter (upstream keeps its reader bar with chapter jump over ReaderFailed); Retry and a chapter change leave the failed state at once, so focus returns to the page. Manga detail's Resume runs once, dims while the saved chapter is looked up, and never replaces a reader already up. Music album/artist/playlist pages open on Play when the dock is showing (the ring started on the dock and stayed there); lyrics scroll to the sung line when the tab opens (music-now-playing.tsx scroll deps). Checked clean: kid content paths (recommendations, collection and franchise through kids-filter; no Continue Watching or Top Shelf; Watch Together invites only in the adult shell; PiP browse off), the parent PIN gate and Menu at the kids root, music queue end / Spotify end / background audio / Menu from Now Playing, manga paging and resume parity, eBook resume and failed card. Open: a deep link under a kid opens any title (upstream parity; owner decision); music started before a switch to a kid has no dock there; shuffle/repeat modes not ported. Device check: music page focus with the dock, lyrics scroll on open, manga failed card. Offline smoke 948.
2026-09-25 10:10 UTC: Seventh adversarial review (subagent, 2b5bafa..8142ac5 plus a716631/fd3e5d6: Browse open items, review 6, Detail/Search/Library pass 2, Settings pass 2). Three regressions fixed (cd5e678): a picker pick made as a partial landed could fail "no-such-stream" because upstream's mergeAndDedupe credits a hash or URL to whoever listed it first (the debrid library once it answers), so the row's streamIdentity changed while the stream stayed; pickedStream now matches the key without its addon prefix (hash ignoring case, URL exactly, never a bare title; smoke). Home servers pulled the ring back to Add Plex from wherever the viewer had gone when a Jellyfin connect returned after its first sync or a Plex code expired or was approved (refocusIfInFlow moves it only from the step), and its Menu handler is a typed optional. Home rows / Anime rows blanked under the ring when a SettingsFieldWatch re-read failed. Checked clean: same-key rows and url-less streams, the Kids switcher's ids, the press-held resolve and P2P path, the auto loop's end and cancel, the premiere fallback (specials-only starts on S0E1), metaFailed only without episodes, the spent hint and strip park, the Library tab feed and grid hand-off, Simkl disconnect's profile, rename drafts vs sync, sound audition, deep links from onboarding only, barMovedAt per room, the anime lead lock, Discover parking, the Your library link. Open: an anime partial built after the final can still replace the engine's lastResults; Simkl disconnect leaves useSimklAvatar; a rename whose row a sync removed. Device check: picking while the debrid library answers, Jellyfin connect then walking away, Plex code expiry elsewhere. Offline smoke 948.
2026-09-25 10:04 UTC: Player device-flow pass 2 (subagent, 3eddbee). "Leave the show?" only restarts a video that was playing (Keep watching / Back unpaused a film the viewer had paused; upstream never pauses for it). The remote's Play/Pause under "Pick up where you left off" takes the ring's choice and under the leave dialog is Keep watching (it started the film at 0:00 behind the fork, or under the dialog). Menu putting the transport away takes the ring to the stage, and so does Skip Intro/Recap (the ring fell to the skip pill / X-Ray / a transport chip, so the next Select didn't pause). A closed panel hands the ring back to the rail chip that opened it (bp-player-rail restoreKey); a quality or kid switch leaves it on the stage. The chrome idle wait restarts when playback resumes without a press (use-bp-player-chrome playing). The Subtitles and Audio dialogs follow the file's track list while open (opened early they said "No subtitles" / "one audio track" for good) and drop older trackView answers. A closing player pauses at once, ignores Back/Play-Pause/Select, and waits for its saves at most 2 s (a Stremio push on a dropped connection held the close; a second Back reopened "Leave the show?"); the tick no longer flushes a second save during the close. Set as default speed dims instead of disabling under the ring. Open: stream-bundled subtitles (src.subtitles / addSeedSubtitles; home-server and addon subtitle files) never reach the player; use-started-near-end not ported. Device check: ring after a panel closes, Play/Pause under the fork and the leave dialog, close latency on a Stremio profile offline. Offline smoke 946.
2026-09-25 09:49 UTC: Settings device-flow pass 2 (subagent). Back from Interface (or the ring leaving the column) puts the saved sound pack back; the auditioned one kept playing everywhere (bp-settings.tsx depth cleanup). The Playback preview names html5 AVPlayer like the column. Trakt, Simkl, AniList and MyAnimeList ask before disconnecting (upstream's dialogs), and the old "Connected as" note clears. Simkl Disconnect resets the profile's Simkl settings and clears its caches like simkl-panel.tsx (simkl.disconnect(profileId, linked); smoke). AniList/MAL keep the ring through Connect → code → Disconnect. Home servers: the Plex server list has a Cancel (there was no way out); the ring follows the Plex code, server list and Jellyfin/Emby form back to Add Plex; Menu closes them before leaving Settings; Remove dims while syncing. Home rows / Anime rows re-read a layout synced while open (SettingsFieldWatch); Menu closes their rename editor; Anime "Clear all" hands the ring on. Sports metadata Save dims instead of disabling; Pull now ignores a second press. Open: Harbor/Stremio Sign out and Letterboxd disconnect don't ask first; some Settings copy is English-only; Trakt/Simkl onDisappear cancel under a cover. Device check: Menu in the Home servers form and rename editors, sound reset on Back. Offline smoke 946.
2026-09-25 09:36 UTC: Detail / Search / Library device-flow pass 2 (subagent). Picker: rows keep upstream's streamIdentity across partial re-ranks (engine stamps tvKey; resolve, P2P consent, rememberPlayback, deadRef and the new streamsRoom.autoCandidateKeys follow the key, so a pick made while slower addons answered could play, remember or mark dead another stream; the focused row was rebuilt under the ring; smoke checks), a late partial no longer replaces the final list, auto-play keeps ticking after a failed candidate fired by the search-done tick (the auto step hung for good), Clear filters hands the ring to All, a double press resolves once. Detail: a season without the resume episode opens its strip at the first card (it kept the last season's scroll); an AI search episode hint is spent once something played (Play restarted the hinted episode after an auto-advance); a series whose meta failed says "Couldn't load this title." with Try again, and Play falls back to the premiere (bp-detail play()). Search: a long query shows its end; Ask AI / retry put the ring on the keyboard; a late manga title resolve opens nothing after the viewer moved on. Library: a new tab no longer shows the last tab's grid and count while it loads (spinner, error note on a failed read); a grid emptied under the ring hands it to the selected tab chip (bp-library chip autofocus). Open: sort/type labels vs upstream (A-Z, Shows), the picker's disabled Sort chip, PersonView's "Any rating" button. Device check: picking while addons answer, auto-play fall-through, strip scroll on a season change, Library tab switch to Trakt, ring after emptying the Watchlist. Offline smoke 943.
2026-09-25 09:27 UTC: Sixth adversarial review (subagent, 9b9039d..33f61ee: Together reopen follow-up, Home/Browse, Profiles/account, Live TV sources). Three regressions fixed (fd3e5d6): a deep link that arrived while Who's watching was up closed it into the profile behind it (the chooser now opens over an active profile, and handle(url:) went to the shell from any non-shell stage; the link now waits for the pick, like upstream's picker layer); Movies and streaming-service pages held the ring on the tab bar for up to 3 s because the first-focus seed waited for a Continue Watching answer they never show (cwResolved is set at once there, as bp-movies seeds on its rows); the late seed pulled the ring back off a tab bar the viewer was walking (use-bp-focus stops seeding after interaction; the bar stamps ShellFocus.barMovedAt and the seed skips after it). Checked clean: pullNow sharing / start waiting / stop forgetting (failed or timed-out shared pull, sign-out mid-pull, stop→start), launch prompt vs fixtures, single profile, a kid behind a parent PIN, sign-in rebuilds and returning from background, Who's watching Back/curfew/PIN (no escape), card marks per profile, CW generation guards, the Together reopen flag, Live caches and memo invalidation, focus-request loops, re-add id vs the EPG cache, Settings' sources-only model. Open: an account change without sign-out can share the old account's pull; a Settings Refresh downloads a playlist nobody sees; an Up-and-wait on the bar can still be re-seeded. Device check: deep link over Who's watching, Movies first focus, walking the tabs on a slow Home. Offline smoke 938.
2026-09-25 09:14 UTC: Browse open items (subagent). Anime Resume / More Info act on the title the hero shows and lock it to the lead (bp-anime-hero lead order: hero slide with art and logo, else with art, else the CW card with art, else the first slide, else the first Top Pick) while they hold the ring (lockBpMeta), so the buttons, their meta line and the hero copy name the title they would start instead of the last card focused on the way up. The anime page reads its hero slides from the same animeRoom.page call as its rows (BrowseSource.page) and the hero opens on the lead (seedBpMeta). Discover's lead bands (Queue, People, Awards, Genres, Voyage) park like rail rows (BPRailLeadMark: the rows' park marker and the rail's focusedRow → park path); Up from a parked rail row left the scroll to tvOS and the band's header sat under the top bar. Jump back in on Home and Shows has upstream's "Your library" see-all (hidden while Library is PIN-locked); testShell and testTabHint press Up twice. Home band order (CW, services, addons, live, rows) still differs from bp-home (Live is a lead view, services/addons are rail rows; moving them changes the hero pool, first focus and Collections placement). Device check: anime actions lock, Discover band parking, Your library link reach. Offline smoke 938.
2026-09-25 09:07 UTC: Live TV sources / guide device-flow pass (subagent). Live TV no longer flashes the first-run setup form on each visit before the sources are read. A source pick, Add and Refresh close Sources at once and load behind it (Adding… waited for the whole playlist; a late close shut a reopened sheet), and a switched source starts from the spinner instead of showing the old source's channels under the new name. Remove asks first (source-picker Remove playlist "{name}"?). An M3U always goes through addPlaylist's checks, an Xtream login needs a password and a bare server gets http://, and re-adding a known address keeps its id and so its favourites, pins and EPG matches (engine; smoke checks). The empty state is bp-live's BpLiveEmpty with Try again / Show all channels / Add a playlist. The category list, ids and favourites count are cached and the channel browser memoises its filter (large sources were re-filtered on every now/next merge). The guide reopens on now for a new chip or source (bp-guide resetKey), Right from the channel cell lands on what is airing (use-bp-guide-nav), and Back from the player rings the channel that was playing after zapping. Hide group / Show … keep the ring in the band. Settings' Live TV row opens the Sources sheet over Settings (ST-3). Compile review of the profiles pass: Who's watching's defaultFocus never gets nil (5af6555); fixture launches skip the launch prompt, so UI tests are unaffected. Open: the grid→list swap when the guide fails late; Multiview "All playlists" joins lists per render; no Arabic-aware channel search. Device check: Right→now, focus after zapping, the Remove alert, the Settings Sources cover. Offline smoke 938, bundle 4501 KB.
2026-09-25 09:04 UTC: Profiles / account device-flow pass (subagent). Who's watching keeps the active profile while it is up, like bp-who-is-watching-layer: Back closes it onto that profile (it cleared the profile, so Back closed the app), picking your own profile just closes (no PIN again, parental unlock kept), and the ring starts on the active tile; the PiP layer still deselects. The launch prompt follows lib/profiles.tsx pickerOpen/launchDefault (engine profilesRoom.launchPicker: every launch with several profiles by default, never/15m/30m, "Start as" default; harbor.profile.lastSelectAt stamped; not for a kid behind a parent PIN, so a relaunch can't get around it; smoke checks). The sync notice shows only for the first pull, as bp-who-is-watching-sync (it flashed on every scheduled pull/push and showed "Couldn't reach Harbor" after any push error); the engine forwards status from sync.pullNow (smoke check). A Harbor sign-in no longer runs two first pulls at once (pullNow shared, start waits, stop forgets; smoke checks). Card marks clear on a profile switch and drop a late read for the previous profile. The PIN pad re-seeds "1" after the cool-down. Profile delete runs once. A Harbor account created on the TV shows its one-time recovery code (recovery-reveal; it was dropped). Open: 15/30-min prompts on return not ported; reminders keep the adult profile under a kid; a parent-PIN keypad shares the kid PIN's try counter; a non-primary profile can delete itself; curfew Switch profile unchanged (owner decision). Device check: Back/launch prompt, recovery reveal, keypad after the cool-down. Offline smoke 935, bundle 4501 KB.
2026-09-25 09:00 UTC: Home / Browse rooms device-flow pass (subagent). Home opens on Jump back in: the first focus waits for Continue Watching and the rows (bp-home seedRowRef, 3 s fallback), where it used to land on the first catalog row while the library read was still running, with the CW row then appearing above the ring. Continue Watching re-reads when a page over the room closes, so a card removed in the quick panel leaves (nothing listened for harbor:cw-dismissed) and a title just watched resumes at its new spot. Movies has no CW row (bp-movies), Shows keeps series only without anime (bp-shows), Home keeps 16. The hero cycles on Home only (useBpHeroCycle); on Anime it changed the title under Resume / More Info. The rail re-parks the focused row when rows arrive or leave above it. Quick panel Watchlist uses Harbor's watchlist (actions.setWatchlist) with Saved/toggle. A failed room gets Try again. Shortcuts: a tab hidden only in the tab settings never bounced; a PIN-locked one did, so Discover's All movies / All shows chips are hidden for a locked tab and Settings' sports notice stays in Settings. Calendar filters: Clear all, group Clear and a removed person chip hand the ring on; tracked people in the search stay focusable. Checked clean: hero copy/backdrop layering, park offsets vs copy and awards corner, lift/ring headroom and row z-order in every RoomView room. Open: Discover lead sections not parked; anime actions don't lock to the lead; Home row leads and band order differ from bp-home. Device check: Home first focus, CW refresh, re-park, calendar focus. Offline smoke 920.
2026-09-25 08:46 UTC: Watch Together reopen follow-up (subagent). A room host who closes the player to reopen (Sources, Switch source, Pick another source, the send-back, Previous/Next, auto-advance) and then backs out of the stream picker without playing now tells the room it left the video, as upstream's closePlayer does (clear media, notifyHostLeaving, clearInvite): guests are no longer held paused under a host who isn't watching, and the host role is handed on. The reopening close marks the reopen pending (TogetherModel.setReopenPending); a pick in DetailView/KidsDetailView or any player opening clears it; the picker cover's onDismiss calls abandonReopen(), which sends only if still host in the same room. The player's Switch source / Sources now resume at the last spot the picture reached when mpv reports 0 after its stream died, else at the opening spot. Device check: host cancels the picker after Switch source / Next / auto-advance with a guest present; Switch source after a mid-film failure. Offline smoke 920, bundle 4498 KB.
2026-09-25 08:38 UTC: Fifth adversarial review (subagent, 9d687ae..691d5dd: Kids/onboarding, Settings, Library sort, Trakt, Sports, Watch Together, Manga/eBook, Discover/Voyages/Calendar, Addons). One regression fixed (197feff): a Watch Together host's Switch source / Pick another source from the error card held the room at 0:00 (mpv reports 0/0 once its stream has died), so every guest jumped back to the start; the reopening close now holds the room at the last spot the picture reached. Checked clean: no harbor:addons-changed storm (only Swift sends it after explicit changes), catalogFor newest-wins + one-shot cached re-read, the Discover band and deck sharing one order, every non-reopening player close still sends host-leaving (Menu, curfew, error Back, PiP, Stop, sleep timer), Sports consent "" vs the tab gate, no settings-updated loop from the librarySort mark, onboarding resume vs a Harbor sign-in rebuild, Shelf → book cover order, seeAllLabel, no Menu swallowed at a root. Open: a host who reopens and then backs out of the picker leaves guests paused under a host who isn't watching (no host-leaving); Set up Live TV / All movies set app.room directly (a hidden tab bounces back to Home via leaveHiddenRoom); the error card's Switch source can resume at 0 after mpv's stream died. Offline smoke 920.
19:45 (09-24): Calendar Custom filter: people can be added on the TV (people-field.tsx: `calendar.customPeopleSearch` over upstream searchAll, 8 results after 220 ms; `calendar.customAddPerson` as config-rail addPerson, role any; sheet with phone typing; tracked people shown disabled). Offline smoke 525.
19:30 (09-24): Upstream re-sync to beta-branch f289f8f3 (28 commits; subagent, docs/upstream-resync-2026-09-24.md): top-bar tab editing on upstream's sidebar model (`engine/navEdit.ts` over settings.navCustomization; order + hides; long-press a tab → Hide this tab / Show all tabs / Reset layout; Settings → Tabs panel; Home stays first and visible, Search keeps its slot, no saved order keeps the Big Picture order); the retired Hide manga / Hide Live TV switches migrate into tab hides (upstream settings/load.ts), parental copies only anime/sports/adult; MinUI Dark (13 presets); watchlist fixes flow through the rebuild, the bookmark mark counts Trakt/Simkl, `cards.removeFromWatchlist` clears every cloud copy. Lottie player + animation JSON from nav-items stubbed. Merge fixes: ShellView `Room.shellTabs` takes both `ebookOn` and `nav`; the ebooks batch's `?raw` text rule now skips asset files and resolves `@/` (upstream's new nav-glyph.tsx imports `@/assets/nav-icons/*.svg?raw`). Review 21 (ebooks): EPUB reader caps total inflate (300 MB), entries (10k) and nesting depth (256). Bundle 4008 KB, offline smoke 522.
19:00 (09-24): Stage 13 eBooks (subagent, docs/ebook-spec.md): Project Gutenberg through upstream's Gutendex source (local/HTML/extension sources need Tauri/DOMParser/Worker and are skipped); native EPUB reading (Compression-framework ZIP reader, forgiving XML/HTML tree, line-for-line port of upstream parseEpub/readEpubChapter; the engine keeps upstream's text cleaning, paragraph split, chapter ids and progress keys so progress matches desktop); TextKit pagination off the main thread; room (setup, Gutenberg quick add, Favorites, Continue your bookmarks, Popular, Shelf, paged searchable grid), detail (Start/Continue, Bookmark, favourite, source picker, chapters, More by, Recommended), reader (Left/Right pages across chapters, chapters/bookmarks/settings panels), AVSpeechSynthesizer narration paragraph by paragraph; "Read the eBook" on anime (light novels); tab hidden until Settings → eBooks (per-TV) and by the anime parental lock. Bundle 3988 KB, offline smoke 510.
18:40 (09-24): Translation coverage 2 (subagent): String-typed copy in Kids, Manga, Live, Social, Together, Music, Sports, Calendar, Wrapped, Collections, Library and Search now goes through T() or upstream format keys (port wording switched to upstream's where they differed); `tools/l10n_coverage.mjs` reports untranslated literals and placeholder-only keys like "%@ %@" (7 found and fixed, incl. PlayPickerView and AnimeAwardView). Those areas 67.7% → 83.6%, whole app 64.5% → 73.0% (literals with a German key); the rest has no upstream key. Offline smoke 485.
18:20 (09-24): Music batch 2 (subagent): Navidrome/Subsonic sign-in (Connections; phone-typeable; only token + salt kept under `harbor.subsonic.v1.*` in the Keychain tier), shelves, search, album/artist/playlist pages, streaming (transcode to MP3 when AVPlayer can't play the codec), now-playing + scrobble reports; Last.fm scrobbling (own API key/secret, approve on the phone by QR, half-length/4-minute rule); ListenBrainz fresh releases via MusicBrainz on the New releases shelf; track radio with auto-extension; synced LRCLIB lyrics tab on Now Playing with ±0.25 s nudge. Internet radio and ListenBrainz scrobbling are not upstream features. Bundle ≈3.83 MB, offline smoke 485.
18:00 (09-24): Player batch 2 (subagent): AVPlayer draws added subtitles itself (`subtitles.cues` runs upstream's SRT/VTT/ASS-to-text parser, Hide HI/SDH; `NativeSubtitles.swift` per subtitle-overlay.tsx with the Look settings and manual offset; kid profiles get upstream's larger bold type), so Find more, Sync, Look and "2nd" (added subtitles only, as upstream's html5) work on the native engine; embedded subtitles still render in AVPlayer. Kid profiles get `PlayerKids.swift`: kids transport, kids resume prompt, "Pick a video" source switcher (six sources, swaps at the same position). Skipped: kids loading screen, volume slider, fullscreen button, clock. Bundle 3749 KB, offline smoke 462.
17:30 (09-24): Translation pass (subagent): `tools/build_locales.mjs` (run in both CI jobs' engine step) turns upstream's 15 catalogs into `App/Locales/<lang>.lproj` Localizable.strings + .stringsdict (plurals) + engine JSON (gitignored, 36.5 MB raw; Swift interpolation keys emitted as %@ / %lld with positional specifiers). The engine now gets the catalog through `settingsRoom.installUiCatalog` (load-locale stays stubbed) and three glue files' English-only t() stand-ins use upstream's. The UI follows uiLanguage via `.environment(\.locale)` (root rebuilds on a language change, like a theme change), `T()` (App/Sources/App/L10n.swift) for String-typed text (tab/hint bars, Settings, onboarding, player chips, Detail actions, dialogs, empty states), right-to-left for Arabic. ~77% of Swift literals match an upstream key. Needs device: runtime language switch of `Text("…")` via the locale environment (fallback: Bundle swizzle), covers, plurals, Arabic RTL, app size. Review 18 (social): socket close cleanup. Review 19 (music): closing Now Playing no longer deactivates the shared audio session (nothing reactivated it); starting music does not stop a playing video (upstream stopCastOwner) — unreachable today because video is always a full-screen cover. Bundle 3732 KB, offline smoke 458.
17:00 (09-24): Stage 12 music core (subagent): upstream's plain-HTTPS Rust connectors ported to `engine/musicSources.ts` (Deezer/iTunes catalog for browse/search, SoundCloud behind upstream's consent with the scraped client id re-scraped on 401/403, Jellyfin and Plex music through the Home servers sign-ins; search fan-out/merge, row order, chart-track → playable-source matching as upstream). Music tab before Live TV (`engine/music.ts`, `App/Sources/Music/`): home rails, search (keyboard or phone), album/artist/playlist/station pages, Sources screen, player bar, Now Playing with queue. AVQueuePlayer with source failover, dead-track skipping, gapless preload in the last 30 s, Now Playing info, remote commands, background audio (`audio` UIBackgroundMode; films pause and Multiview goes quiet when the app leaves the foreground). Spotify (librespot), YouTube Music and local files documented in docs/music-spec.md, not built. Bundle 3736 KB, offline smoke 457.
16:30 (09-24): Stage 10 social + Watch Together (subagent): top-bar account menu (bell with unread badge, room icon while in a room; profile, notifications, activity, groups, Watch together, shared list, Who's watching, Settings/Sign in; kids get Who's watching + Settings only). `engine/social.ts` over upstream lib/social: profile page (banner, avatar, handle, level, presence, stats, friend actions, watching now, lists, badges, activity, friends, comments with phone typing), notifications (accept/decline, detail, mark all read, clear, 60 s badge refresh), activity feed, groups (tags, phone-typed search/posts, join/leave/invites, likes, members), shared lists (link, handle/list, `harbor://list/…`). `engine/together.ts` runs upstream's room client over a new WebSocket shim (`EngineSockets.swift`): public or custom relay, start/join by code or invite (auto-join after 4 s), invite QR, presence, chat (quick replies or phone), in-player sync via `TogetherPlayback` (play/pause/seek, lobby, guest presses to host, view-only drawings) for mpv and AVPlayer. Not tested against the live account or relay. Skipped: group/profile editing, drawing, host-source matching, non-1× speed. UI test presses 14 → 20 to reach the cog. Bundle 3642 KB, offline smoke 443.
16:00 (09-24): Stage 13 manga (subagent): Suwayomi servers through upstream lib/manga (`engine/manga.ts`, `manga.*`; plugin/HTML/Mangayomi sources need Worker/IndexedDB/DOMParser and are skipped). Manga tab (hidden unless Settings → Manga is on; also hidden by the anime parental lock): add a server, hero, Continue Reading, Popular, Library, browse grid with source chips and search; detail with Read latest/Resume/Start, favourite, chapters by language, order, 50-chapter ranges, read marks; TV reader (long strip, single/double pages, right-to-left, fit, zoom and pan, Play/Pause zoom steps, Select bar, auto next chapter, tall pages go long-strip, prefetch, bounded page cache, progress/up-next/server sync as upstream). Search Manga row and chip (SR-9), anime detail "Read the Manga". The Suwayomi server list (it holds passwords) is routed to the Keychain; active source keys are durable. Skipped: plugins, AniList rows, book/horizontal modes, phone page-turner, ebooks. Bundle 3496 KB, offline smoke 397.
15:40 (09-24): Per-profile gating (subagent): `engine/parental.ts` + `Profiles/ParentalGate.swift` port the Big Picture top-bar locks — a PIN profile's lockedTabs (Anime, Shows, Movies, Sports, Library, Calendar) disappear until the PIN is entered this session (Who's watching or the profile editor), re-lock on switch/relaunch, the tab bar, shoulder buttons and every entry point fall back to Home; hideContent is copied into the profile's settings on switch/sync (hide anime drops the tab and anime from rows, Continue Watching, search, recommendations; search also drops locked anime/Live TV); profile editor "PIN & sidebar locks" saves the desktop shape. Settings "Remove PIN" now asks for the PIN first (it bypassed the locks). Discover/Live TV never lock, as upstream. Bundle 3358 KB, offline smoke 371.
15:20 (09-24): AVPlayer engine (subagent, PLAN decision 4): `player.pickEngine/engineFor` port use-player-bridge / player-utils pickBridge (AVPlayer stands in for upstream's html5; stored values stay auto|mpv|html5): Auto sends web-ready HLS and web-ready Dolby Vision mp4/m4v/mov to AVPlayer, the rest to mpv; a fixed setting wins (live stays on mpv when mpv is chosen, unlike upstream). `NativePlayerController`/`NativePlayerView` (AVPlayerViewController with hidden controls under Harbor's chrome; display matching by AVKit; headers + user agent; resume seek; audio/subtitle language picks; undecodable-audio detection) behind `PlayerEngineControlling` (mpv conforms by extension). Auto falls back to mpv once at the same position on codec/audio failure; forced AVPlayer shows "Use mpv engine". Anime4K, audio delay, Find more/Sync/Look and "2nd" hidden on AVPlayer. Skipped: PiP, AVPlayer subtitle renderer. Bundle 3352 KB, offline smoke 359.
15:00 (09-24): Settings/onboarding/themes batch (subagent): ST-1 Live TV row counts playlists from lib/iptv/playlists-store (upstream moves them out of settings.iptvPlaylists); ST-4 settings live preview (`settingsRoom.pane`, bp-settings-pane: overscan crop, subtitle sample with flags, Harbor-vs-Classic wireframe, services, greeting, summary) with categories → controls → Back; OB-2 16-language onboarding picker writing `uiLanguage` + `setUiLanguage` (engine i18n re-applied on settings reload / profile switch; Swift strings not translated yet); OB-5 upstream step order and copy, TMDB/Stremio showcases, layout/subtitle previews, done flourish; SH-7 bp-intro poster wall after the splash (5–8 s, skippable, remembered posters, UI tests skip it); Stage 9 themes: `engine/themes.ts`, 12 upstream presets + font pairs in Settings → Appearance saved to settings.theme (syncs), BP colours/fonts read from the active theme (Harbor default unchanged), gradients, Aurora bokeh, crunch/glossy buttons, glass panels, light MinUI/Kawaii. Themes mix in sRGB, not oklab. A theme change during onboarding waits until it ends. Review of calendar/multiview/kids: two reported compile errors were false positives (optional `var` members default to nil). Bundle 3350 KB, offline smoke 355.
14:40 (09-24): Stage 7 Kids (subagent): kid profiles are selectable in Who's watching and get `KidsShellView` instead of ShellView (App.tsx pin): Watch / Play tabs, profile chip, clock; leaving a kid profile with a parent PIN asks for it (use-account-menu). `engine/kids.ts` (`kidsRoom.*`): kids-specs page (movies ≤ PG, no horror/thriller, Cinemeta fallback without TMDB), kid-safe filters, row customisation, paging, hero logos, 14 "Pick a World" franchises + pages, kids detail with episodes. Swift `App/Sources/Kids/*`: hero, rows, franchise rail, Play Zone banner + doodles (3 of 4 activities; the Scratch arcade needs a web view), kids detail (Play goes to the best source, seasons, collection, More to explore), franchise grid; deep links open kids detail. tools/sync_upstream_assets.sh copies the kids art. Pending: lockedTabs/hideContent tab gating for adult shells, the kid player UI, row edit mode. Bundle 3332 KB, offline smoke 330.
14:20 (09-24): Stage 8 remainder (subagent): Multiview (`Live/MultiviewView.swift`, from the Live band or "Add to Multiview" in the live player; 1/2/Stacked/3/2x2 layouts remembered, Clear all, Hide controls, provider-cap banner; each tile its own mpv in a new `tile` mode + `muted` flag, only the selected tile unmuted, never touches display mode/HDR, torn down when closed/hidden/while the full player is up; per-tile channel picker with source chips, groups, search, pasted URL; retries and "Stream offline" per cell.tsx). In-player channel switching for Live-room channels (TV Guide list, Previous channel over the last 12, "This channel isn't responding" card). Playlists room (`engine/liveVod.ts`, `PlaylistVodView`): M3U/Xtream Movies/Shows tabs with search and paged grid, series → seasons → episodes (Xtream episodes on open), VOD player with local-only resume like upstream. Needs device: four simultaneous decodes on the A15, focus feel, double blank on channel switch with Match Content. Bundle 3312 KB, offline smoke 316.
14:00 (09-24): Calendar and Stats batch (subagent): Calendar tab before Library (`engine/calendar.ts` ports views/calendar*: eight sources, filters, Watchlist only, 42-day month grid, day view, Custom filters, Sub/Dub, week start, poster size, empty-state copy); reminders now surface on tvOS (engine reminder runner: first check after 25 s then every 6 h, toast when one fires, unseen badge on the tab, reminders list from the header bell, "fired since last visit" banner); Stats (Wrapped) from a Library chip (`engine/wrapped.ts`, Left/Right between cards, gated by settings.wrappedButton). UI test presses 12 → 14 to reach the Settings cog. Skipped: adding tracked people (needs TMDB people search), the Detail episode hint, reminder sound/system notifications. Sports review: no defects. CI job timeouts raised to 60/75 min for the first librqbit build. Bundle 3285 KB, offline smoke 292.
13:40 (09-24): Stage 6 torrents (subagent): librqbit 8.1.1 inside harbor-ffi on its own tokio runtime behind a JSON C ABI (`harbor_torrent_*` in include/harbor_ffi.h, strings freed with harbor_string_free, panics caught at the boundary, release profile now panic=unwind); a vendored pure-Rust sha1 wrapper replaces aws-lc-rs so no C crypto builds for tvOS; project.yml links CoreFoundation. `streamsRoom.resolve` returns a P2P plan where upstream would use its local engine (honours torrentsDisabled, directTorrentStream, remoteStreamServerStrict, 2-seeder minimum); the P2P consent dialog is live and auto-play picks torrents only with consent. Swift `TorrentEngine` streams `http://127.0.0.1:<port>/stream/<hash>/<file>` into mpv, the connecting card shows peers/speed/readiness, data under Caches/torrent-engine is deleted 1.2 s after the player closes and swept at start (leftovers capped at 10 GB). Host `cargo test` covers start/add/range reads/stats/remove/shutdown. Staticlib (host) 38 → 110 MB. Needs CI to confirm the tvOS cross-builds and a device check for peers/ports/disk. Skipped: LAN/casting routes, UPnP, session persistence, per-stream disk cap. Review 16 (phone hand-off, security): per-device cap of 4 connections, field details only to the bound phone. Bundle 3167 KB, offline smoke 268.
13:20 (09-24): Home/Discover batch (subagent, HM-1/2/4/5, DS-3): Home Collections row (`collectionsRoom.curatedRow`/`tmdbCard`, 16:9 cards before the third catalog row, 8 s budget, hidden without a TMDB key); Home bands (services, addons, collections, live) take over the backdrop with a band title, a drifting poster collage (`services.posters` / `addonsRoom.bandPosters`), a services colour wash and the collection still; Home Live TV row muted mpv preview (bp-live-hero, 1600 ms dwell, suspended under any cover, failed-channel memory, off with Reduce Motion); hero provider mark and awards corner (`cards.heroAwards`); anime award tiles and overlay in Discover (`discoverRoom.animeAwardSources/animeAward/animeAwardOpen`), "Open the Oscars" tile; Top People open the person page. Award logos are symbols (upstream webp icons not bundled).
13:10 (09-24): Sports parity batch (subagent, SP-1/4/7/8/11/12/13): `engine/sportsEvent.ts` (eventRows, where, actions/toggleReminder/toggleFollow, webhooks/setWebhooks/testWebhook, reminder runner on an engine timer — tvOS cannot send them in the background, apiSports/setApiSportsKey) and watch() plans stream > broadcast > channel (officialBroadcasts, clearAttachedStream). Swift: SportsLiveViews (baseball diamond, football field, basketball court, formation pitch, plays, stat bars, player tables; live summary refreshes every 30 s), SportsWhereView (venue, provider tiles, app-or-QR links; Twitch/YouTube open their tvOS apps, others go to the phone), Settings "Where alerts go" (Discord / Telegram, test) and "Sports metadata" (API-Sports key in the Keychain tier), hero bell/follow. Sports reminders key is durable. Bundle 3117 KB, offline smoke 248.
12:50 (09-24): Stage 10 phone hand-off and phone typing (subagent, SR-7/ST-2/OB-4): `App/Sources/Handoff/` ports upstream's handoff protocol/code/session/host (12-character pairing code, input checks, claim/bind/lockout, one command at a time with a 20 s cap, plus a `text` command for remote typing); `PhoneLinkServer` (NWListener on port 11471 or any free port, only while a panel is open, local-network peers only, 8 KB headers / 16 KB body, serves one bundled page `App/Phone/harbor-phone.html` + `POST /api/remote`, phone polls over HTTP instead of upstream's WebSocket); onboarding phone step, Settings Connect pane, Search "Type on your phone", a phone button on every URL field (BPField), the TMDB key and tracker code fields; engine `account.adopt` signs the TV into the account the phone signed into. NSLocalNetworkUsageDescription added. Needs a real Apple TV: local-network prompt, phone reachability, QR scan, sign-in from a LAN origin. Also: CI run 141 failed because the search batch's collection hits called the old CollectionItemsOverlay signature after the library batch changed it; fixed. Review 15 (shell/library): UI sounds never play during playback. Bundle 2979 KB, offline smoke 224.
12:30 (09-24): Player batch (subagent, PL-2/6/7/10/11/12/13/14): up-next card (countdown ring, Play now / Keep watching, lead from nextEpisodeLead, auto-advance respects autoPlayNextEpisode / Keep watching / 150 s stub; upstream's Play now opens the picker with auto-play, no pre-resolve); Subtitles dialog with Tracks (language rail, Embedded/External, Hide HI/SDH, Forced only, Best match, "2nd" dual subtitles via secondary-sid), Find more (other title / S·E steppers, Show N more), Sync and Look (presets, Size/Height/Opacity, Shadow/Outline/Box/Bold, live sample applied to mpv); Audio dialog with ±0.1/±0.5 s delay; transport row (prev/next episode, 1×/3×/6× held-seek ramp), buffered fill, "Ends HH:MM"; mute chip. Engine `subtitles.trackView/find/titleTarget/presets`, `player.prefs`. DetailView passes `onPreviousEpisode`. Auto-sync / load file skipped (desktop-only). Bundle 2978 KB, offline smoke 220.
12:15 (09-24): Library/Collections batch (subagent): Collections TVDB source (`collectionsRoom.tvdb/tvdbDetail` via Harbor's TVDB proxy, no key; All capped at ten names with "See every TVDB list"; overlay backdrop, card meta lines, end messages); own collections editable from the TV (`collectionsRoom.create/rename/remove/addItem/removeItem/searchTitles/saveCommunity`, desktop editor copy since Big Picture has no editor); Letterboxd public mode (`engine/letterboxd.ts`: Settings → Letterboxd username, Library Letterboxd tab, Movies rows after Top 10; full sign-in mode skipped); Library "Repair library" panel (`libraryRoom.repair` with `harbor:library-repair` progress, `animeScan/animeHeal`). Bundle 2958 KB, offline smoke 212.
12:10 (09-24): Shell chrome batch (subagent, SH-1/4/8/9/10/11/12): theme sounds synthesised in Swift from lib/sfx.ts (`BPSound`: hover/click/close/boot/open/page turn, silent during playback, Sound setting previews on focus), hint bar that follows the input (remote / controller / keyboard) and the room, Back from a tab returns Home, controller-connect toast + shoulder buttons switch tabs, hero cycle honours Reduce Motion and pauses only while a tile has focus (it used to stop for good after the first focus), hero pips, per-route/per-row focus memory, title art settles/decodes/fades with a slow Ken Burns. Band glow tint and the Y button still open.
11:50 (09-24): Detail/picker batch (subagent): hero actions in upstream order (`actions.heroState/toggleFavorite/toggleReminder/setMovieWatched/traktMarkWatched/trackers/trackerSet/trackerRemove`; icon row + hint line, TrackerDialogView; no Download on tvOS; Watch trailer after Rate/Lists/Watched), picker "Played last" pin (`streamsRoom.remembered`), bp-stream-dialogs (P2P consent gated by `streamsRoom.p2pConsentNeeded`, dormant until a torrent engine; debrid down after two debrid-side failures with same-source retries; no sources; auto exhausted; resolve failures carry upstream copy), episode still ladder (`detailRoom.episodeArt` → EpisodeStill with the numbered plate), episode select auto-plays under instant play. Reminders are saved but nothing fires them on tvOS yet. Reminder and movie-watched keys are durable. Bundle 2921 KB, offline smoke 186.
11:30 (09-24): Live TV batch (subagent): manual EPG match (LV-3: `live.epgCandidates`/`live.setEpgMatch`, epg-match-modal port; `EpgMatchView` from the list row's link button or holding Select on a guide row; empty lanes say "No program info · Match EPG"; the lane and now/next follow the match), Big Picture filter chips from use-live-home rails (Continue watching, pinned groups, country/theme rails, top groups, hidden groups skipped, flags, no zero counts), VOD lines filtered out + group-relevance sort (use-bp-live), programme iconUrl feeds the guide preview. Live TV epgmap/groupPrefs/countryPrefs/stats keys are now durable. Bundle 2854 KB, offline smoke 175.
11:15 (09-24): Search parity (SR-4/5/8, subagent batch): kind chips with counts (use-bp-search: kept for the whole query, reset on a new query, "Searching" / "{n} results", note when a chip goes empty), TVDB collection banners (`search.fanOut` collections; `search.collection` opens the list in the Collections items overlay), "Addons you could install" Select opens the addon page and holding Select installs, rows in upstream order, pending addon answers tracked. SR-9 manga skipped (no reader on TV). Review 14 (batches 13-16) found no defects; its two parity notes are applied (addon panel no-response notice; trailer action order goes with the Detail batch). Bundle 2842 KB, offline smoke 161.
10:45 (09-24): Guide floating preview (LV-1, bp-guide-portal): `GuidePortalView` in the guide's bottom-right corner (programme icon, else channel logo over its blurred copy; Live label, title, range · category, description, progress). After a 700 ms dwell a muted mini mpv (`MPVPlayerController.preview`: mute, aid=no, 4 s cache, no display-criteria/HDR changes, no track prefs, never resets the real player's criteria) fades in; a failed channel is not retried this session; suspended while the player, a replay or the Sources sheet is up (IPTV one-connection caps); hidden while the focused row sits under it; off with Reduce Motion. The old bottom text band is gone. Not hydrated with Cinemeta art yet (use-channel-hydration). Untested on device: two mpv instances back to back.
10:15 (09-24): Sports addon sources (SP-3, bp-sports-addon-*): `sports.addonSources` (gatherCatalogAddons → refreshSportsAddonManifests → loadSportsAddonListings; consent-gated, 45 s memo per game+account), `sports.addonStreams` (loadSportsAddonStreams), `sports.addonPlay` (direct link → resolveStream → live player; external page → phone QR; torrent / non-http / catalogue title → handoff to the regular stream picker). `sports.watch(game, addons)` follows bp-sports-watch's plan order (channel if exact or pinned → addons if matched → picker → addons if any → setup). Swift: `SportsAddonPanelView` (listings with Browse/filter/pages of 24/Refresh, streams, handoff), Addon sources row (8 matching + Browse tile), picker button, resolveWatch sequence guard. Bundle 2836 KB, offline smoke 157.
09:30 (09-24): Detail Videos row (DT-10, detail/bp-videos-row): `detailRoom.extras.videos` now follows upstream's clip list (other trailer candidates first, then TMDB extras, deduped, 14); 16:9 YouTube-thumbnail cards after "You Might Also Like"; "Watch trailer" action (TMDB lead trailer, else Cinemeta/Jikan `trailerStreams`). `TrailerView` replaces bp-trailer's yt-dlp playback (not available on tvOS): Open in YouTube (`youtube://watch/<id>`, then the universal link) or scan the QR code on a phone. Shared `QRCode.image`. Bundle 2817 KB, offline smoke 148.
09:00 (09-24): Sports who panel wired (bp-sports-event-hero BpEventSide): `sports.whoSides` says which sides have a profile subject; those sides are buttons ("Open {name}") that open `SportsWhoView` (team profile, roster → athlete, Back/Close). Side sub-line shows rank + record. Bundle 2817 KB, offline smoke 147.
04:30 (09-24): Batch 12: gallery row (`detailRoom.gallery`: TMDB backdrops / posters / logos, 24 each) with a Left/Right lightbox, seasons sheet for long runs (bp-season-menu; chips stay for up to eight seasons). Offline smoke 145.
04:00 (09-24): Batch 11: title awards row + per-award dialog (`detailRoom.awards`: Wikidata live entries merged with the bundled history, one cell per award body), sports personalize step 3 "Follow your teams" (`sports.teamLeagues/teams/favouriteTeams`, picks saved on select; the engine's "Your teams" row already follows them). Bundle 2776 KB, offline smoke 145.
03:30 (09-24): Batch 10: per-episode facts chip (`detailRoom.episodeFacts`: TMDB vote/runtime + Harbor IMDb episode ratings), Sports direct play for an exact live match (useBpWatchGame: card/hero Select tunes the channel when the plan is "channel"), where-to-watch provider tiles with marks, structured Live TV setup (M3U / Xtream Codes server+login / guide-only, `live.addStructured` through materializePlaylistEntry). Fixed the CI break from batch 8 (SportsModel.Game type name). Bundle 2763 KB, offline smoke 142.
03:00 (09-24): Batch 9: stream picker loosen ladder ("Search wider" drops strict mode, "Show everything" disables the filter; settings.streamFilterLevel seeds the start), crew cells open the Person page (extras.crew carries TMDB ids), facts card opens a full FactsDialogView, the episode strip scrolls to the resume episode. Offline smoke 141.
02:30 (09-24): Review 12 (batches 4-5) applied: hero MAL score resolves kitsu ids through kitsuToMal and only says "MAL" when it is one (`fromMal`), "N min left" gets the raw duration/offset (ContinueItem.durationMs/timeOffsetMs), the dub badge waits for the dub set, anime row customisation is per profile, manga rating type, no empty Simkl spec, bare infohashes never become recents. Batch 8: sports standings table (`sports.standings` → StandingsSection under the event's watch section), TheSportsDB artwork fallback for cards with no art (`sports.artwork`), Live TV channel pins (`live.toggleChannelPin`, pin button beside the star) and hidden channel groups (`live.toggleGroupHidden`, "Hide group" / "N hidden" chips; guide order honours them). Bundle 2760 KB, offline smoke 141.
01:45 (09-24): Parity audit batch 7: Home Live TV row (`live.homeRow` ranks the active playlist through bp-live-rank: favourites first, junk names penalised, most-watched/network boosts, now/next + progress; LiveRowView tunes on Select, "Guide" opens the Live room). Bundle 2713 KB, offline smoke 139, network smoke 219.
01:00 (09-24): Parity audit batch 6: Continue Watching extras (`rooms.continueWatchingWithExtras`: Trakt/Simkl "currently watching" merged per cwSources/cwPerProfile, Trakt watched check, "+N new" episodes since the last watch, Up Next, waiting-for-air countdown, "Watched by <profile>"; ContinueCardView shows them), ambient poster mosaic behind screens without art (`BPMosaicView` from the hero feed pool, gated by bigPictureMosaic), `harbor://detail/<type>/<id>` + `stremio://detail/...` deep links open the title and `stremio://<host>/manifest.json` installs the addon (CFBundleURLTypes, AppModel.handle), anime detail path (`engine/animeDetail.ts` runs lib/providers/anime-detail's Kitsu chain: episodes as PlayEpisodes with kitsu stream ids, AniList characters row; DetailModel branches on kitsu/mal/anilist/anidb ids), TMDB curated collections (`collectionsRoom.tmdb`: the 110-franchise catalog resolved 12 a page, four lanes, by category; "TMDB" source + category chips in Collections; TVDB lists still pending). Bundle 2704 KB, offline smoke 138.
00:30 (09-24): Parity audit batch 5: provider score chips (`engine/scores.ts` ports use-bp-card-badges: IMDb via harbor-imdb/OMDb/Cinemeta, TMDB vote, MDBList batch, Simkl proxy, RT/audience/Metacritic/Letterboxd/Trakt, card vs detail gates → ScoreChipsView on the hero and the detail page), Home "Your addons" band (`engine/addonsRoom.ts` cards with cloud merge + posters; brand tiles with logos) and the addon page (catalog chips + paged grid), anime hero actions + meta line (`animeRoom.heroMeta`: award pill / New, MAL score, Sub and Dub, country, episode, minutes left). Review 11 applied: switch/pick-another reopen the picker in manual mode, History defaults to Posters, kid profiles prefer 1080p in auto-fire, "addon order" sorts by installed order + nativeIdx (engine returns addonOrder, streams carry addonUrl/nativeIdx), HDRip/DVDRip/SCR source aliases, auto-resolve honours cancellation, recents record only on commit (no magnets/URLs), idle suggestions pool Home rows. Bundle 2594 KB, offline smoke 134.
23:45: Parity audit batch 4: curfew guard (`CurfewState` counts a kid profile's daily allowance while playing, `CurfewLockView` "Time's up!" with parent PIN / switch profile, topmost in RootView, the player exits when it locks), onboarding "Your taste" step (`engine/onboarding.ts` tasteTitles/vote/upvoted, five picks written on select), `engine/actions.ts`: custom lists (list / toggle / new) → ListDialogView, ratings (rate / unrate / rating, synced through harbor.site when signed in) → RateDialogView, both on Detail and in the quick panel; Anime rows panel in Settings (move / hide / rename / reset through lib/anime-customization, the room rebuilds on `harbor:anime-updated`); Collections overlay reports hidden manga items. Bundle 2573 KB, offline smoke 131.
23:15: Parity audit batch 3: instant play (`streamsRoom.autoCandidates` ports use-auto-candidates; the picker auto-fires after use-auto-fire's settle windows with "Choose instead", up to three tries; `streamsRoom.rememberPlayback` saves the pick + season lock; Detail "Play" fires, "Sources" never does; auto-advance fires too), in-player "Sources" switch (resumes at the same position via `PlaybackContext.explicitStartSec`), subtitle manual offset (±0.1/±1 s, reset) and size stepper (mpv sub-delay/sub-scale), connecting card (elapsed, Go back / Try again / Switch source after 8 s), Xtream short-EPG fallback (`live.loadShortEpg`, cap 120), picker facet chips (Source/Codec/HDR/Audio with counts), sort toggle (Harbor / addon order), Clear filters, onboarding "Your services" step, search recent-query chips + idle "Suggested" row, Library History Episodes/Posters toggle (`episodes` feed input). Review 10 applied (Back on the leave dialog unpauses, genre pages skip filtered-empty pages, live errors keep the chrome). Bundle 2560 KB, offline smoke 124.
22:30: Parity audit batch 2: genre grid page (`discoverRoom.genrePage` → CatalogPageView "genre:" rows with the four use-bp-genre-grid states, "Surprise me" lead tile), Discovery Queue deck (`engine/discover.ts` queueOpen/queueExtend/queueSnooze/queueBlock; QueueDeckView with Play now / Save / Details / Skip / Not interested, Left/Right steps), quick panel (hold Select on a rail tile → QuickPanelView: Play, Watchlist, Details, Remove from Continue watching via `rooms.dismissContinueWatching`, Search seeds the Search room), screensaver (ActivityMonitor swizzles UIWindow.sendEvent for the idle clock; ScreensaverModel after screensaverDelayMin idle minutes rotates `feed.hero` art with "#N in {list} today"; first press wakes and is swallowed; suppressed during playback), stream error card (mpv end-file error → "Pick another source" reopens the picker / "Try again" reloads / Back), channel labels (`bp-guide-title` bpChannelLabel/bpGroupLabel in `live.ts` toView → label + quality badge), guide focus panel (bp-guide-portal text half under the grid: programme, channel, time range, progress, description), DetailView(autoPlay:). Bundle 2545 KB, offline smoke 121.
21:45: Ninth review (home servers) applied: sync keeps upstream's detailed lastSyncResult and records failures, `homeServers.startRunner` (App.tsx MediaServerSyncRunner: due connections at launch + every 15 min, started in AppModel.boot), progress write-back (`homeServers.reportProgress/stopPlayback`; PlayerScreen reports every 15 s / on pause / watched at ≥90 % / stops the transcode session), a copy's server position is the explicit start, quality + refresh pickers on the panel. Parity audit batch 1: kid profiles selectable in Who's Watching (bpWhoKidSelectable is true on a TV shell), resume prompt (settings.resumePrompt, ≥30 s) + resumePlayback honoured, leave confirmation (playerConfirmLeave, "Don't ask again"), three Playback toggles in Settings, skip pill no longer steals focus (Up reaches it), Setup rows say "Connected: …" / "{n} added", top-bar CloudOff/WifiOff glyphs (NetworkStatus), Sports consent no longer promises reminders. Search fan-out moved into `engine/search.ts` (`search.fanOut`: TMDB + anime + addon catalogs fused + Cinemeta + Live TV + AniList franchise + addon index; slow addon groups stream as `harbor:search-addon-group`), Franchise rows and "Addons you could install" (Select installs) in Search. Bundle 2535 KB, offline smoke 119, network smoke 196.
21:00: Home media servers (Stage 6, lib/media-server): `engine/media/transport.ts` (fetch replaces the Tauri command) and `engine/media/index-store.ts` (localStorage replaces IndexedDB) stand in for upstream via bundle-config redirects; `engine/homeServers.ts` = Plex plex.tv PIN flow (start/poll/pick server), Jellyfin/Emby connect, sync with `harbor:media-server-sync` progress, `copies` for the picker, `play` (direct/transcode URL + headers + subtitles). Swift: Settings › Home servers panel, "On your home servers" section in the stream picker (`PlayPickerView.onPlay` stream is now optional), Library › Media servers tab, connection/mapping/summary keys durable. Bundle 2580 KB. Parity audit landed at `docs/parity-audit-2026-09-23.md` (top 15 ranked).
20:45: Eighth review applied: tracker list loads retry after failure, Library chip counts come from the group-scoped set and the Trakt cache is keyed by session token, anime `failed` uses the raw fetched count, anime CW honours cwPerProfile shared-session hiding and includes Simkl playback, addon rows cache per profile, favorites/custom lists/local watchlist are durable, service posters dedupe in flight, auto-fill budget 4. Catch-up replays deliberately use the VOD cache profile (bounded, seekable).
Engine: `cd engine && npm test` = 117 shim + 71 smoke checks (live network; Jikan check skips when api.jikan.moe is down). Glue files: rooms.ts, discover.ts, streams.ts, player.ts, subtitles.ts, live.ts.
Docs: browse-spec, big-picture-design, harbor-protocol, engine-report, detail-spec, player-spec, livetv-spec.

## Key files
- `PLAN.md` — full plan: architecture, 15 stages (0–14), tvOS limits, open decisions.
- `reference/harbor` — shallow clone of `harborstremio/harbor` `beta-branch` @ `f289f8f3` (was 1bfcfb6 until 09-24) (will become a submodule).

## Decisions
- SwiftUI UI modelled on upstream `src/views/big-picture`.
- HarborEngine: upstream `src/lib` TS bundled into JavaScriptCore (to be proven in Stage 0.4).
- Rust (`harbor-core`, librqbit, subsync) as tvOS static libs; `aarch64-apple-tvos` present in rustup here.
- Player: MPVKit (libmpv) + AVPlayer Auto mode.
- Repo: private on GitHub to start.
- Build: XcodeGen + GitHub Actions macOS → TestFlight internal only. CI simulator screenshots for UI review.
- Harbor account API: `harbor.site/identity/api/*`, sync `sync.harbor.site/sync/v1/{state,push}`. Sync client starts read-only.

## Next (pick up here — updated 2026-09-25, session on branch `claude/determined-hopper-smvu37`)
**Where things stand**
- All of 09-24's work (sports who panel → translation pass, see Status) is on the branch `claude/determined-hopper-smvu37`, not on `main`. `Build` only runs by itself on `main`, so each batch was built with a manual `workflow_dispatch` on the branch. **Run 187 (64c59af) is green and is on TestFlight as build 187 (0.1.0 (187), uploaded 2026-09-25 03:52 UTC at the owner's request; before it the live build was 79 from 09-23 — every upload from run 81 through 130 was refused by Apple's daily upload limit, code 90382): the whole branch through today's bug passes (player 1+2 + tracks, music 1+2, shell/storage, browse 1+2, decode hardening, Together/Settings, Kids/Sports + follow-up, lifecycle, eBook/X-Ray/Calendar, Live TV/sources, onboarding/profiles, the addon-dialog and chapter-read follow-ups, music sources/manga, browse 3, one-press resume timing, performance, localization, Settings/Social, performance pass 2) compiles and the UI tests pass**; run 172 (b8f1c40) was the green build through review 38 — the 09-24 parity audit batches, Continue Watching advance + anime Top Picks, AI search, X-Ray, Voyages, browsing Harbor during PiP, music volume, the Kids Play Zone arcade and reviews 22–38. (Runs 162/163 each failed on one compile error: PlayPickerView argument order in DetailView, then `updatesNowPlayingInfoCenter`, unavailable on tvOS.) Run 183 (e68f1c5) failed; the compile check of the three passes had found a trailing `//` comment in HomeRowsPanel that swallowed the Reset button's action (fixed in 4aefa4b). Run 189 (e71ab0a, mosaic + accessibility passes) is green too. After it: the hero/z-order fix (784f5cc, TestFlight build 190) and the lead-row fix (850bd86, TestFlight build 191, uploaded 04:28 UTC; run 191 green, UI tests pass). Run 193 (f8e3f7a: f19ee64 mosaic skip + the visual layout pass) is green; build 194 (d17a9c3, with d87ac72) is green and was uploaded to TestFlight at 05:02 UTC — the current TestFlight build. Run 196 (fd586dc: focus navigation pass, 2b97d6d, focus pass 2) is green; build 197 (85f0f97, same code) is green and on TestFlight (uploaded ~05:33 UTC; the current TestFlight build). Run 198 (d004b0d, the Detail bug pass) is green; build 199 (a124aa4: Detail pass + the Stremio removed/temp fix) is green and on TestFlight (uploaded ~06:00 UTC; the current TestFlight build). Run 200 (cb43b40, the player / Live TV device-flow pass) is green; build 201 (27ef1f8, same code) is green and on TestFlight (uploaded ~06:31 UTC; the current TestFlight build). Run 202 (5563503, the Music device-flow pass) is green. Run/build 203 (e0f1a90: Music + Search/Library passes + 9ea2a21) is green (UI tests pass) and on TestFlight (uploaded ~06:56 UTC; the current TestFlight build). After it: the Kids/onboarding pass (run 204 green) and the Settings pass; build 205 (ecf059a, both) is green (UI tests pass) and on TestFlight (uploaded ~07:30 UTC; the current TestFlight build). Build 207 (fe4d3e4 Library sort vs in-flight feed, 05125a2 librarySort sync mark, 1080fd2 Trakt Disconnect guard, and the Sports device-flow pass) is green (UI tests pass) and on TestFlight as build 207 (083ec3b, uploaded ~07:49 UTC). Build 208 (b4e5807: the Watch Together pass + its paused-reopen follow-up) is green (UI tests pass) and on TestFlight (uploaded ~08:05 UTC; the current TestFlight build). Build 210 (the Manga/eBook and Discover/Voyages/Calendar passes + 36b4d8f PersonView spinner-first) is green (UI tests pass) and on TestFlight as build 210 (ddced74, uploaded ~08:24 UTC). Build 211 (the Addons device-flow pass) is on TestFlight (uploaded 08:40 UTC; UI tests green). Run 212 (197feff, compile check) green 08:51 UTC. Build 213 (the Together reopen follow-up + 197feff) is on TestFlight (uploaded ~09:07 UTC; UI tests green). Build 214 (the Home, Profiles and Live TV sources passes) is on TestFlight (uploaded ~09:25 UTC; UI tests green). Build 215 (Browse open items a716631 + review 6 fd3e5d6) is on TestFlight (uploaded 09:40 UTC; UI tests green). Build 216 (Detail / Search / Library pass 2 2392e15 + fb19d4a) is on TestFlight (uploaded ~09:56 UTC; UI tests green). Build 217 (Settings pass 2 ae63dfe + 8142ac5) is on TestFlight (uploaded ~10:15 UTC; UI tests green). Build 218 (Player pass 2 3eddbee, review 7 cd5e678, Kids/Music/Manga pass 2 73a636e) is on TestFlight (uploaded 10:30 UTC; UI tests green). Build 219 (review 7 open items 7e017d0, stream-bundled subtitles 7202a85, device-flow pass 3 9d9b369) is on TestFlight (uploaded ~10:50 UTC; UI tests green). Build 220 (review 8 6622588, perf/memory pass 37aa626) is on TestFlight (uploaded ~11:07 UTC; UI tests green). Run 221 (Watch Together pass 2 a8ac757, Discover/Voyages/Calendar/onboarding pass 2 a3ed212) compiled and passed UI tests, but its upload hit Apple's daily limit (90382, 11:22 UTC): build 220 stays the newest on TestFlight until the limit resets (about a day). Until then runs are compile checks (testflight=false); the next upload should carry everything since 220. Run 222 (42a70eb: player clock, review 9, Detail room invite, localization) green 11:43 UTC. Run 223 (9c46340: tracker notes, VoiceOver pass 2) green 12:04 UTC. Run 224 (298012f: review 10, Library owned sorts, Media Servers details and rows) green 12:29 UTC. Run 225 (248ee45: review 11, Sports/Addons pass 2) green 12:57 UTC. Run 226 (49e9e1f: Social/Collections/Reminders pass, review 11 open items incl. lazy storage) green 13:12 UTC. Run 227 (a0e027d: review 12, music shuffle/repeat) green 13:33 UTC. Run 228 (efdb1bc: review 13) green 13:49 UTC. (Run 152 failed on two ebook compile errors: NSLayoutManager temporary attributes are macOS-only, and a mutating call on a temporary; fixed in da22a6b.)
- **Owner action needed:** merge the branch into `main` (or open a PR), then run `Build` with `testflight=true` from GitHub Actions (only the repository owner can). Build 187 (everything through 64c59af) is on TestFlight since 2026-09-25 03:52 UTC; upload again with `testflight=true` (at most a handful per day — Apple refuses more with code 90382).
- Spotify (librespot) is merged and links for tvOS (run 157); it needs a Premium device check.
**Then**
1. Device checklist for the owner (nothing below has run on real hardware): torrent streaming (peers, ports, disk), phone hand-off (Local Network prompt, QR, sign-in from the LAN page), Multiview 4 decodes, AVPlayer Auto mode (HLS / Dolby Vision, fallback to mpv), guide and Home live previews, UI sounds, themes, manga reader memory.
   Full consolidated list (smoke pass first, then by area, with prerequisites): `docs/device-checklist.md`.
2. Translation: whole app ~80% (de); Music 97% / Player 95%; Kids/Manga/Live/Social/Together/EBook/Calendar/Sports ~86%. New screens since (AI search, X-Ray, Voyages, Addons manager, Home rows / Tune anime panels) reuse upstream keys. `tools/locales-tvos.json` needs a native-speaker pass. Verify on device that the locale environment switches `Text("…")` at runtime.
3. Still open: Top Shelf (needs a second signed target + an App Group; the owner must add the extension's provisioning profile in App Store Connect and the CI signing step), the desktop "Harbor on TV" settings mirror (stored by sync, applied nowhere; no upstream consumer either). Blocked on tvOS: subtitle Auto sync (needs subsync + audio extraction in harbor-ffi), hero trailers (yt-dlp), seek thumbnails (native thumbnailer), YouTube Music (web view). The 09-24 parity audit (docs/parity-audit-2026-09-24.md) items 1–11, 14 and the Lower list are ported (see Status 00:30–03:30 09-25); most reviews' "Left:" items were fixed in the 03:45 leftovers batch.
4. Keep the pattern: batch → `node build.mjs` (≈3.5 MB now) → `node smoke.mjs --offline` (≈400 checks) → commit → push → dispatch `Build` on the branch → fix red → fresh-context review (a subagent that has not seen the work) → apply. When several subagents work in parallel, each must merge the latest branch and grep every caller of any shared signature it changed before its final commit (CI run 141 broke on exactly that).
